# REVIEW TASK_PACKET PKT-A1-R2 — Verification of FIX_PROPOSED findings (FC-W1→epoch 2) + independent audit of PC03/PC04 (FC-W2 epoch 2)

- packet_id: `PKT-A1-R2` · assignee: `auditor-A1` · authority `AUTH-COORD-A1-R2` · lease null (read-only)
- report path: `…/scratchpad/audits/A1-R2-report.md` · helper scripts under `…/scratchpad/a1/` · PYTHONDONTWRITEBYTECODE=1 · no repo writes, no git mutation, no network
- Frozen candidate FC-W2 epoch 2 manifest: `…/scratchpad/audits/FC-W2-manifest.txt` (80 entries, manifest_sha256 `e605c73aef55ad41124662af01fad4bd2259bd12e904fab9add7e9d88349599c`). Recompute at start and end. Files outside the manifest under `contracts/http/`, `contracts/ops/{secrets,internet-boundary,backup-restore,collector-probe,cli-acp-probe}.md`, `contracts/ai/`, `contracts/ui/`, `contracts/telegram/`, `contracts/schemas/{worker-assignment,ingest-receipt,analysis-result,saved-snapshot}.schema.json`, `acceptance/fixtures/{collection,ai,telegram,recovery}/`, `evidence/handoffs/PC05..08-*` belong to PC05–PC08 (in progress, out of scope) — ignore them.
- completion_ceiling: DRAFT_FOR_REVIEW. audit_route rationale: cross-module state/transaction contracts (HIGH), unverified fixes to MAJOR findings.

## Part A — Verification verdicts for F-A1R1-01 … F-A1R1-09
Inputs: your report `A1-R1-report.md`; Coordinator rulings `…/scratchpad/packets/FIX-A1R1-rulings.md`; fix addenda in `evidence/handoffs/PC00-handoff.md` (PKT-PC00-FIX1), `PC01-handoff.md` (PKT-PC01-FIX2), `PC02-handoff.md` (PKT-PC02-FIX2). For each finding: re-run your original reproduction against epoch-2 bytes; verdict `VERIFIED | NOT_VERIFIED | PARTIAL` with evidence. Note where a fix diverged from the ruling (e.g. R-01 "single row update" implemented as an append-only row — CR-PC02-12; `MOD-data-admin-service` created — PROV-PC01-06) and say whether the divergence still satisfies the remediation constraint. You do not close findings.

## Part B — Independent audit of PC03 and PC04
Packets: `…/scratchpad/packets/PC03-packet.md`, `PC04-packet.md` (requirements each had) plus the Coordinator notes in their dispatch (R-01 alignment for PC03; CR-PC02-06 and CR-PC03-06 for PC04). Method as in PKT-A1-R1 §Method, plus:
1. **State machines**: re-run a transition lint of your own (enums, reachability, terminals without outgoing rows, every plan §8.1/§8.3 row present, forbidden transitions listed); spec §9.1 enum mapping vs PC00 AMD-B02 (13 rows) verbatim; delivery.yaml vs plan §8.3 verbatim; storage.yaml refusals; every error code in errors.yaml has target state + retry_class + oracle; every plan §10 code present; retry-policy budgets all have number+unit+rationale and the worker/adapter split does not multiply retries (D43). Judge W4's deliberate divergence (one automatic re-run from `analysis.unknown_attempt`) against plan §8.2/§10 and the packet.
2. **Checkpoint (R-01)**: PC03 CP-01/CP-07/FT-CHECKPOINT-ONLY vs PC02 TXN-checkpoint-only vs PC01 `ingest.commit_checkpoint` — one consistent account, one oracle.
3. **Reporting**: tag freeze at publish, half-open contiguous coverage (re-run the contiguity check), pending/late ledger, backfill exactly-once incl. add→remove→re-add (W5 keyed by normalised tag text — judge vs D28 and B04), manual rescan, first-announcement after merge (CR-PC02-06 resolution) vs I07/AC-09, density definition and the worked example (recompute), `insufficient_evidence` handling (B14), threshold `PROVISIONAL_BOOTSTRAP` labelling vs REQ-OQ08 and baseline "no TBD", generation pinning (I12), empty period choice (b) with `abort_reason` (CR-PC03-06) and its replay argument in time-and-tags.md §4.7.1 — is the argument sound and is `report.schema.json` consistent (status const published)?
4. Cross-package: every operation/entity/error/state cited by PC03/PC04 exists upstream (script); PC04 fixtures' `expected.report` validate against report.schema.json with target `$ref` resolved; fixture actors are allowed callers (same gate as R-02); header fields; language; no claim above DRAFT_FOR_REVIEW; no `TBD`/`CLOSED`/`ACCEPTED`.
5. Evidence honesty of both handoffs; re-run any PASS you can.

## Report
Same structure as R1 (frozen ref/epoch/manifest hash recomputed; independence; scope; evidence; findings `F-A1R2-nn` with severity/condition/expected-observed/impact/remediation constraint; Part A verdict table; per-package verdicts for PC03/PC04 and epoch-2 verdicts for PC00/PC01/PC02; limitations; ceiling). Reply to the Coordinator ONLY: Part A verdicts in one line each; per-package verdicts; finding counts by severity; five most important findings one line each; report path (≤30 lines).
