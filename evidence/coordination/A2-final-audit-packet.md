# REVIEW TASK_PACKET PKT-A2-R1 — Final independent audit of the complete Pre-code baseline (FC-W4 epoch 4)

- packet_id: `PKT-A2-R1` · assignee: `auditor-A2` (fresh principal; has authored nothing and has not seen A1's reports until instructed below) · authority `AUTH-COORD-A2-R1` · lease null (read-only)
- report path: `…/scratchpad/audits/A2-R1-report.md` · helpers under `…/scratchpad/a2/` · PYTHONDONTWRITEBYTECODE=1 · no repo writes, no git mutation, no network
- Frozen candidate FC-W4 epoch 4 manifest: `…/scratchpad/audits/FC-W4-manifest.txt` (manifest_sha256 in header). Recompute at start and end; any drift → STALE for the affected scope.
- completion_ceiling: DRAFT_FOR_REVIEW. audit_route rationale: this is the claim-bearing review before the Coordinator reports to the Owner; whole-baseline scope; residual uncertainty about cross-package consistency after four fix waves.

## Inputs (read in this order)
1. `…/scratchpad/packets/00-coordination-baseline.md` (session rules, ID conventions, provisional decisions), `agent_profile/auditor.md`, `protocol.md`, `registry.json`.
2. Sources SRC-PLAN, SRC-SPEC in full (hashes in baseline §2).
3. `precode/README.md`, `precode/review.md`, `precode/gates.yaml`, `precode/owner-decision-request.md`, `precode/decision-register.md`, `precode/requirements.csv`, `precode/baseline.json`, `precode/adr/*`, `precode/change-control.md`.
4. All of `contracts/`, `acceptance/`, `agent-tasks/`, `evidence/` (handoffs with all FIX addenda, `evidence/index.json`, `evidence/runs/*`, `evidence/tools/e0_check.py`, `evidence/manifest.schema.json`).
5. Only AFTER you have formed your own view: A1's reports `…/scratchpad/audits/A1-R1-report.md`, `A1-R2-report.md`, `A1-R3-report.md` and the Coordinator rulings `FIX-A1R1-rulings.md`, `FIX3-rulings.md`, `FIX4-rulings.md` — to (a) verify that every A1 finding is either VERIFIED-fixed by your own reproduction or still OPEN, and (b) judge whether the rulings were applied consistently.

## Method
A. **Run E0 yourself**: execute `evidence/tools/e0_check.py` from your scratch dir (read-only mode; if the tool writes into the repo by default, do NOT run that mode — inspect and report BLOCKED for that check instead) and compare its output with `evidence/runs/E0-*.json` and `evidence/index.json`. Any mismatch between recorded and reproduced results is a finding.
B. **Plan §17 Definition of Ready, item by item**, each with your own evidence: spec snapshot/hash + atomic registry; XN/UQ/P0 mapping and ĐX/KC status; B01–B17 explicit scoped block (PROVISIONAL, never CLOSED); module ownership/ports/denied edges + negative cases; every operation has schema/auth/transaction/idempotency/concurrency/error/evidence; every error code has target state/recovery/forbidden behaviour; coverage/backfill/pending/tag version/identity/analysis/Saved oracles for race/crash; Telegram unknown, CLI capability, backup WAL/restore, secrets unambiguous; AC-01–18 + SC catalogue with fixtures/oracles/evidence level; E0 run with manifest, E1–E4 NOT_RUN; cards pin baseline/stack/contracts/proofs; readiness report per module.
C. **Traceability sampling**: pick 30 requirements at random across categories from requirements.csv → traceability.csv → contract → scenario → fixture; verify each link resolves and the fixture oracle actually tests the requirement. Pick all 18 AC. Pick every invariant I01–I15: positive + negative scenario exist and fixtures encode them.
D. **Cross-package consistency** (script where possible): operation ids (ports.yaml) cited anywhere exist; entity/field names (entities.yaml) cited in fixtures/contracts exist (R4-01 rule: `_` annotations, `pending_cr`); error codes (errors.yaml); states (contracts/state/*.yaml); scenario ids anchored in baseline.json and present in scenarios.yaml; fixture actors are allowed callers (`operation` key, `edge_assertion: forbidden`); security scheme convergence PC05/PC07/PC08; header fields per baseline §3 (with the declared exceptions); language rule; no `TBD`; no status `CLOSED`/`ACCEPTED`; no claim above DRAFT_FOR_REVIEW anywhere; B01–B17 PROVISIONAL everywhere.
E. **Task cards**: every card's contract hashes recompute; operation/SC ids resolve; ten sections present; stop conditions carry the known KC/BLOCKED items; no card is "build everything"; walkthrough conclusion (can an agent work from card + refs alone?) — form your own view on one collector and one delivery card.
F. **Evidence honesty**: every EV record in every handoff that you can re-run, re-run; every PASS must reproduce; NOT_RUN where nothing ran; no "independent audit passed" written by a Worker; `evidence/index.json` validates against `evidence/manifest.schema.json`.
G. **Owner decision request**: every PROVISIONAL decision in the decision register and in handoffs' unresolved refs appears in `precode/owner-decision-request.md` (set comparison); each section has question/options/recommendation/blocked scope.
H. **Claim assessment**: for each module in review.md, is READY_FOR_CARD / BLOCKED justified by evidence? Is the conditional CONTRACT_READY statement correctly scoped (only scopes whose blockers are resolved-pending-ratification and whose E0 checks pass)?

## Report
AUDIT_REPORT with: frozen ref/epoch/manifest hash (recomputed per protocol §6); reviewer identity + independence declaration; scope checked/excluded; evidence records with commands and exit codes; findings `F-A2R1-nn` (severity, file/ref, condition, expected/observed, impact, remediation constraint — no patches); verification table for every A1 finding (F-A1R1-01…09, F-A1R2-01…06, F-A1R3-01…05): VERIFIED / NOT_VERIFIED / PARTIAL by your own reproduction; DoR table (plan §17) with MET / MET_PROVISIONAL / NOT_MET and evidence; per-package verdicts PC00–PC10; overall verdict; limitations; completion ceiling statement and your view of which scopes may be called CONTRACT_READY upon Owner ratification. Reply to the Coordinator ONLY: overall verdict; per-package verdicts; DoR summary (met/provisional/not met counts); finding counts by severity; five most important findings one line each; report path (≤30 lines).
