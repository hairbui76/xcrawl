# REVIEW TASK_PACKET PKT-A3-P3-R1 — independent review of Phase 3 (M3) + Phase 5 (M6, plain-text scope) — FC-P3

- packet_id: `PKT-A3-P3-R1` · assignee: `auditor-A3` · authority `AUTH-COORD-A3-P3-R1` (parent `AUTH-OWNER-20260908-10`) · lease null (read-only; git-ignored artefacts only; `git status --porcelain` before/after)
- report path: `…/scratchpad/audits/A3-P3-R1-report.md`
- Frozen candidate FC-P3: `…/scratchpad/audits/FC-P3-manifest.txt` — manifest_sha256 and entry count are in its header (filled at freeze time; recompute at start and end; nothing should be writing to the repo — precode/contracts frozen, all six card leases released).

## What's new since FC-P2 — verify all of it independently
1. **Six cards implemented in one wave, disjoint §3 write sets, precode frozen** (no overlapping leases this time — check that claim against the handoffs' lease timestamps and the file ownership):
   - Phase 3 / M3: `TC-analysis-adapter-validation` (`worker/app/adapter/*`), `TC-analysis-once-per-generation` (`server/app/analysis/*`, migration `0006_tc_analysis_once_per_generation`), `TC-embedding-generation-switch` (`server/app/embedding/*`, migration `0006_tc_embedding_generation_switch`).
   - Phase 5 / M6, **plain-text only** per `OD-20260908-09`: `TC-telegram-linking-auth` (`server/app/telegram/*`, migrations `0006_tc_telegram_linking_auth` + `0007_merge_phase3_5_heads`), `TC-saved-snapshot` (`server/app/saved/*`, `0008_tc_saved_snapshot`), `TC-telegram-unknown-delivery` (`server/app/delivery/*`, `0009_tc_telegram_unknown_delivery`).
   Each claims `IMPLEMENTATION_VERIFIED` (card scope) at SELF_VALIDATION, except adapter's CLI/ACP path (`CONTRACT_READY`, `REQ-AC16` BLOCKED).
2. **AI provider decisions**: `REQ-OQ03` resolved by `OD-20260908-06` (Claude Sonnet 5 → `label`+`direction_phrasing`, Claude Opus 5 → `summary`, both `api_key`); `REQ-A5` Owner-signed `OD-20260908-08` (Anthropic ToS/Usage Policy/Service-Specific Terms read 2026-09-07T18:08Z, `permitted_for_this_use`, `TC-A5-01` warranty accepted). **Both adapters `enabled: false`** (ISO-03/ISO-05 unverified, E3 NOT_RUN). Verify: the code reads `providers.yaml` as data and refuses before transport/credential/host while disabled; **no configuration in the repo makes a live call**; canary secret never leaves; tool calls 0; egress ⊆ allowlist; `usage.unknown` never written as 0 (I14).
3. **Telegram scope**: `contracts/telegram/delivery.md` 0.2.1 §3.4 — 2/5 facts resolved (4096 chars; 1/s per chat, 20/min group, ~30/s broadcast), 3 BLOCKED_DEPENDENCY. Verify **no `parse_mode`, no inline keyboard, no `callback_data` parser** exists anywhere in the three Telegram/saved packages (grep the whole diff, not just the handoffs); splitting counts as the contract locks; every blocked path is `NOT_RUN (BLOCKED_DEPENDENCY CR-PC07-04)`, not stubbed-and-hidden; unknown chat ⇒ 0 outbound / 0 mutation / 0 reply; `unknown` delivery exits only via `delivery.decide_unknown`; 0 automatic resends after a possible send; permanent failure leaves the report hash unchanged.
4. **Migrations**: five new revisions + one merge; one head `0009_tc_telegram_unknown_delivery`. Force both branch orders from a blank DB; run `tests/contract/test_schema_matches_entities.py`; check `0002b` tables were *taken over* (saved_snapshot, saved_item, analysis, work_label), not recreated; the added FK `saved_snapshot.analysis_id_at_save → analysis(id)` is real (rebuild) and contract-declared.
5. **Cross-card integration in-wave**: W5B integrated W5A's `CMD-save` and identity's `merge_works` for real; W3B integrated W3A's `TaskInput`; W5C read W5A's `linking.active_link` in a test-side adapter and imports nothing from `server.app.telegram`. Verify the xfails are exactly the cross-phase ones (pending report-coverage-publish-cas / scheduler-lease-claim / backup-restore-drill) — 11 xfailed repo-wide, none of them hiding an in-wave failure.
6. **Precode round**: `owner-decisions-05…09.md`, `decision-register.md` §8.14.x/§8.15.x, `providers.yaml` 0.2.0, `entities.yaml` wording-only note (verify structurally: no field/constraint change — WP could not measure that), `requirements.csv`/`traceability.csv` KC→XN for REQ-OQ03/REQ-A6/D34/S13.2-01, `AMD-SPEC-D34-01`, registry authorities. Verify the spec file is byte-unchanged. Independently re-derive at least one Anthropic terms fact and one Telegram fact from the allowed docs hosts (same docs-only grants: `anthropic.com`/`www.anthropic.com`; `core.telegram.org`), confirming the recorded quotes.
7. **Lease-overlap incidents** (`CR-PC00-32`): earlier in this round WT and WAI held overlapping leases twice; the Coordinator verified no data loss by reading files. Independently confirm on the final bytes that both workers' sections are complete and nothing was clobbered (§8.14.1–3 and §8.15–8.15.1; `CR-PC07-04/.a/.b` and `CR-PC06-OQ03-01…08`).

## Method
Full regression from a clean env (expect 817 passed / 11 xfailed / 0 failed — verify exactly), ruff, mypy --strict, web tests, e0 (25/25), verify_cards (13/13 @ the P3b epoch), OpenAPI validator, alembic one head from blank DB in both orders; the socket-patched no-network run again over the whole suite; per-card claim verification with the fixtures each card names (NOT_RUN honestly justified); whole-diff scan for undeclared writes, scope creep, lease violations; the six workers' CR lists — flag any CR that is actually a defect the worker should have stopped on (a "declared deviation" that hides a real gap).

## Report
Verification table; per-card claim verdicts; scope verdicts (AI disabled-by-default, Telegram plain-text-only); overall; new findings `F-A3-P3-nn` with severity; residual list. Reply ONLY: overall verdict, per-card verdicts, key numbers, new findings count + severities, report path (≤30 lines).
