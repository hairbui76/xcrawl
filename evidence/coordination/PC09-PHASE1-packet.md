# TASK_PACKET PKT-PC09-P1 — Register Phase 0/1 evidence; gates; E0 rule updates

- packet_id: `PKT-PC09-P1` · assignee: Worker `worker-W6n` (verification owner, successor of W6) · authority `AUTH-COORD-PC09-P1` (parent `AUTH-OWNER-20260907-03`) · lease `LEASE-PC09-e10` · expires 2026-09-08T16:00Z · ceiling for the baseline docs: DRAFT_FOR_REVIEW; for the code cards: whatever A3 verified, never more.
- Inputs: `evidence/handoffs/P0-skeleton-handoff.md`, the four `TC-*-handoff.md`, their `evidence/runs/TC-*-E1-*.json`, `…/scratchpad/audits/A3-R1-report.md` (+ any A3-R2), `evidence/handoffs/PC00-handoff.md` (FIX15/16), `evidence/handoffs/PC10-handoff.md` (FIX15), `precode/gates.yaml`, `precode/review.md`, `evidence/index.json`, `evidence/tools/e0_check.py`, `evidence/tools/README.md`, `docs/master-plan.md` §3 Phase 0/1 DoD.

## Scope
1. `evidence/index.json`: register the skeleton and card evidence manifests (E1/E2 records with the A3 verdict as `review_type: INDEPENDENT_AUDIT` references, the Workers' own as SELF_VALIDATION); `e1_e4_run` counters updated honestly (E1 and E2 now > 0 for the covered scenarios; E3/E4 still 0).
2. `precode/gates.yaml`: G5 — `G5-X4` (repo layout) met by Phase 0; G5 status per its remaining conditions (REQ-OQ03 still open → G5 `PARTIALLY_MET` unless all conditions are met; state which); add Phase-1 evidence refs to G6 conditions (still NOT_MET: no integration/live); `ratified_contract_scopes` unchanged.
3. `acceptance/scenarios.yaml` / `traceability.csv`: scenarios exercised by the card tests move from `NOT_RUN` to `PASS (E1)` / `PASS (E2)` **only** where A3 reproduced them; keep `NOT_RUN` elsewhere; the fixture-as-test-data assumption from the master plan is now either confirmed or refuted — say which, with the loader path.
4. `evidence/tools/e0_check.py`: (a) CR-P0-02 — `E0-12` permits claim labels up to `IMPLEMENTATION_VERIFIED` inside `evidence/handoffs/**` and `evidence/runs/**` when the record cites an A3 report; contracts/ and precode/ rules unchanged; (b) CR-P0-05 — add a CI-run test for the fixture loader (or record that the Phase 1 cards' tests already exercise it, with the file names); (c) add `E0-19-generated-matches`: `shared/rr_contracts/…/GENERATED_FROM.json` and `web/src/generated/` source hashes equal the current contracts (the generate-don't-hand-edit rule as an E0 check); (d) scan dirs stay contract-only — code is checked by pytest/vitest, not E0.
5a. Contract-round CR consolidation: the five Phase 0/1 handoffs raised 32 CRs against contracts/fixtures/schemas (list extracted at `…/scratchpad/phase1-cr-consolidated.txt`); add every one to the CR table with owner package and a proposed disposition (fix in next contract round / accept as limitation / duplicate of an existing CR), flag the ones that change a CONTRACT_READY file (they will need a re-freeze and A2 verification when applied), and record the Coordinator rulings already made: manifest schema cap correct (IMPLEMENTATION_VERIFIED only via A3 record); storage probe table absent → CR to PC02/PC03; lockout counter in-process → CR to PC02 (entity) with the restart-clears limitation disclosed; cross-card FK enforcement → CR to PC02/PC10 (coordinated rebuild card).
5. `precode/review.md`: new §"Phase 0/1" — what exists, what A3 verified, per-card claim, the honest test counts, what remains NOT_RUN; module readiness re-evaluated for the modules those cards implement; CR table additions (CR-P0-01..05, CR-PC00-20/21, card CRs, A3 findings with dispositions).
6. Closing E0 run → new run file; numbers/cr_summary regenerated; index updated.

Rules: no edits to code, contracts, cards; CRs for anything wrong there. Addendum "PKT-PC09-P1" to `evidence/handoffs/PC09-handoff.md`; release lease. Reply ONLY: status, files with after-hashes, E0 counts, gate statuses, scenario status counts (NOT_RUN / PASS-E1 / PASS-E2), residual (≤20 lines).
