# Phase 4 (M4/M5) + Phase 6 (M7/M8) card dispatch — prepared 2026-09-08, dispatched after FC-P3 audit PASS + PC09 + commit

Common rules: identical to `PHASE3-5-card-dispatch.md` "Common rules" (which extend `PHASE1-card-dispatch-template.md` items 1–8): WAIT GATE on the epoch named at dispatch time + released PC10 addendum; SG-HASH before first write; write set = card §3 exactly + your Alembic revision(s) (ONE head, merge if needed, `len(heads)==1` structurally) + handoff + `evidence/runs/<card>-E1-<UTC>.json`; `server/app/main.py` one delimited block; nothing under `precode/`, `contracts/`, `acceptance/`, `agent-tasks/` (freeze; defects → `CR-<card>-nn`); cross-card deps absent → port + `xfail(reason="pending TC-…")`; siblings present → integrate for real; network only for `uv`/`npm` installs; **no live X/AI/Telegram calls, no browser automation against real sites, no secrets, no git mutation**; `PYTHONDONTWRITEBYTECODE=1`; scratch `…/scratchpad/<worker>/`; quality bar + handoff + reply format as template. Authority: Owner instruction 2026-09-08 "do until finish everything" under `AUTH-OWNER-20260908-10`; Phase 4 entry (Phases 1 and 3 done) and Phase 6 entry (1, 4, 5 done) are satisfied at the card level by the FC-P3 audit; where a dependency's claim is still short of its ceiling, scope your own claim accordingly and say so.

Repo state at dispatch: Phases 0–3 and 5 (plain-text) on `main`; existing packages: `server/app/{auth,ingest,identity,storage,research,analysis,embedding,telegram,saved,delivery}`, `worker/app/adapter`, `collector/app`, `probe/`, `web/` (Vite + React + TS skeleton, generated client in `web/src/generated/`, `web/src/lib/api.ts` exists as a skeleton file — the UI cards own it per CR-P0-04). Tables created by earlier cards on behalf of later ones: `first_announced_ledger` (0002b, for report), `assignment_lease` (0006 analysis, custodian — scheduler card must **extend, not recreate**, CR-TC-ANALYSIS-01), `outbox_intent`/`delivery*` (0009). Known cross-card CRs you inherit: CR-TC-embedding-01 (`work_label.embedding_generation_id` FK needs a rebuild of a table the report card may own), CR-TC-DELIVERY-06/08 (`delivery.report_id` NOT NULL / FK deferred to the report card), CR-TC-SAVED-07 (`report_item` table absent — W5A's `/save <report_item_id>` waits on it), CR-TC-ANALYSIS-02 (`assignment_lease.run_id` NOT NULL meaningless for analysis claims), CR-TC-adapter-06 (`types-PyYAML`/`types-jsonschema` dev deps).

## Phase 4 — M4 reporting, M5 emerging directions, UI
### W4A — `agent-tasks/TC-report-coverage-publish-cas.md` (land first; W4B, W4D, W6B integrate with it)
Lease `LEASE-TC-REPORT-e1`, authority `AUTH-COORD-TC-REPORT`. §3: `server/app/report/{builder,publisher,coverage,router}.py`, `tests/contract/test_report_schema.py`, `tests/integration/{test_publish_cas,test_coverage_contiguous}.py`. Oracles: coverage windows half-open `[from,to)`, contiguous, no overlap, no gap — including empty periods and after a publisher crash (E2, `db/faults.py`); two concurrent publishers ⇒ CAS on predecessor, exactly one wins; published report immutable (hash unchanged); `report.schema.json`; selection per `contracts/reporting/selection.md` on a versioned snapshot input; embedding generation guard consulted before the publish commit (W3C's port — turn its xfail real by exercising it from your side if feasible; it stays W3C's test). Depends on identity (done), embedding (done), analysis (done), backfill (W4B, sibling — pending-item ledger through the port, xfail if absent). Take ownership of `first_announced_ledger` (0002b); create `report`, `report_item`, and what the contract names; resolve the deferred `delivery.report_id` FK if it is a rebuild of your own table only, else CR.

### W4B — `agent-tasks/TC-backfill-pending-ledger.md` (wait-gate: W4A handoff released)
Lease `LEASE-TC-BACKFILL-e1`, authority `AUTH-COORD-TC-BACKFILL`. §3: `server/app/report/{backfill,pending}.py`, `server/app/tags/rescan.py`, `tests/integration/{test_backfill_once,test_pending_survives_cursor}.py`. Oracles: add → remove → re-add entitles exactly once (`backfill_ledger`, CR-PC04-02 — if the column the card needs is genuinely missing from the contract, stop with the CR as the card says); builder crash does not consume the backfill; cursor advances, pending items survive; `tag.rescan_corpus` uses its own rescan ledger; tag change ⇒ analysis provider-call delta 0 (W3B's key). Shares the `server/app/report/` package with W4A — disjoint files; import W4A's coverage/publisher, never edit them.

### W4C — `agent-tasks/TC-ui-runs-three-states.md` (creates `web/src/lib/api.ts` content; W4D extends after)
Lease `LEASE-TC-UIRUNS-e1`, authority `AUTH-COORD-TC-UIRUNS`. §3: `web/src/lib/{api,runState}.ts`, `web/src/routes/{runs,runDetail}.ts`, `web/src/views/{RunsList,RunDetail}.tsx`, `web/tests/contract/runReadModel.test.ts`, `web/tests/integration/threeStatesDistinct.test.ts`. Client generated from `contracts/http/openapi.yaml` via `openapi-typescript` (`web/src/generated/` — regenerate only through the existing `web/scripts/generate.mjs`; hand-write nothing wire-shaped); cookie session + `X-CSRF-Token` on every mutation. Oracles: empty / limited / failed produce **three different display strings** (compare strings, not screenshots); `needs_user`, `blocked`, delivery `unknown` distinguishable; state triple `(status, outcome, stop_reason)` per `contracts/state/run.yaml` + `contracts/ui/screens.yaml`. Vitest + eslint + tsc clean. Backend deps scheduler (Phase 6, W6A in the same wave — integrate through the generated client against fixture responses; no live server needed) and delivery (done). Render review on desktop/mobile is E4 → NOT_RUN (a human looks).

### W4D — `agent-tasks/TC-ui-reports-detail.md` (wait-gate: W4C handoff released — you extend `web/src/lib/api.ts`, don't recreate)
Lease `LEASE-TC-UIREPORTS-e1`, authority `AUTH-COORD-TC-UIREPORTS`. §3: `web/src/lib/{api (extend only),provenance}.ts`, `web/src/routes/{reports,reportDetail,workDetail}.ts`, `web/src/views/ReportDetail.tsx`, `web/tests/contract/reportReadModel.test.ts`, `web/tests/integration/provenanceLabels.test.ts`. Oracles: the UI shows the same analysis revision as the Telegram payload (fixture `ui/sc10-same-analysis-revision-app-and-telegram.json`); provenance labels `author_claim | source_verified | ai_inference` + `comparator: unknown` never collapsed; evidence-level labels per `contracts/reporting/report.schema.json`; saved-snapshot detail (W5B, done).

## Phase 6 — M7 scheduler/catch-up, M8 backup/restore
### W6A — `agent-tasks/TC-scheduler-lease-claim.md`
Lease `LEASE-TC-SCHED-e1`, authority `AUTH-COORD-TC-SCHED`. §3: `server/app/scheduler/evaluator.py`, `server/app/jobs/{service,lease,router}.py`, `tests/contract/test_worker_assignment_schema.py`, `tests/integration/{test_scheduler_catchup,test_lease_two_claimants}.py`. Oracles (fake clock injectable, `Asia/Ho_Chi_Minh`, DST fixture `collection/j`): three offline periods ⇒ **exactly one** catch-up run (`job.coalesce_overdue`); two claimants ⇒ `COUNT(assignment_lease active)=1`, stale epoch rejected (`STALE_LEASE`); `run.run_now` while active (fixture `collection/i`); `run.cancel` from every non-terminal (fixture `k`); heartbeat/TTL per `retry-policy.yaml`. **Extend** the custodian `assignment_lease` table from `0006_tc_analysis_once_per_generation` (CR-TC-ANALYSIS-01/-02 — make `run_id` nullable only if entities.yaml permits; else CR and refuse). Consumers: collector (`worker.*` stub in W?C's tests becomes real — do not edit collector files; note the integration for the audit), UI runs (W4C), Telegram `run-now` (W5A xfail → their test, not yours).

### W6B — `agent-tasks/TC-backup-restore-drill.md` (wait-gate: W4A released — I15 reconciliation touches outbox + report; W5C's drill-driven xfail is theirs)
Lease `LEASE-TC-BACKUP-e1`, authority `AUTH-COORD-TC-BACKUP`. §3: `server/app/backup/{snapshot,verify,restore,reconcile}.py`, `tools/backup_cli.py`, `tests/integration/{test_wal_unsafe_detected,test_restore_side_effect_lock}.py`. Oracles: SQLite Online Backup API / `VACUUM INTO` + manifest; WAL-unsafe copy **refused** (fixture `recovery/d`); after restore, `COUNT(telegram outbound)=0` until reconciliation completes (`recovery/a`, `j`, `m`); stale lease rejected post-restore (`recovery/b`); `saved_snapshot.content_hash` before = after (`recovery/e`); `restore_record` operator confirmation (CR-PC08-05 — stop with CR if the field is missing from the contract); storage `recovery_required` hook (WR's interface) wired; `backupOperatorToken` only for the CLI. **The real restore drill (E3, RPO/RTO measured) needs the Owner's environment — NOT_RUN here**; everything else E1/E2.

## Dispatch note (2026-09-08, appended at dispatch time)
Dispatched while A3-P3-R2 / PC09-P3 / packaging / commit run, to overlap the reading phase. **WAIT GATE for every Phase 4/6 worker (no write before all three hold):** (1) `git log -1 --format=%s` on `main` shows a subject beginning `feat: Phase 3` (the FC-P3 commit); (2) `agent-tasks/README.md` declares epoch `PC10-PIN-P3b-20260908` or a later epoch whose PC10-handoff addendum shows a released lease; (3) your card's §0 pins match disk (SG-HASH). Poll every 60 s, up to 3 h; read everything meanwhile. Per-card gates on siblings' handoffs apply in addition (W4B ← W4A, W4D ← W4C, W6B ← W4A).
Assignments: W4A/W4B/W4C/W4D = new spawns (7–10 of 10 this round); W6A = worker-W3C reused; W6B = worker-W5B reused.
