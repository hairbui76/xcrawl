# REVIEW TASK_PACKET PKT-A3-P4-R1 — independent review of Phase 4 (M4/M5) + Phase 6 (M7/M8) — FC-P4

- packet_id `PKT-A3-P4-R1` · assignee `auditor-A3` · authority `AUTH-COORD-A3-P4-R1` (parent `AUTH-OWNER-20260908-11`) · lease null (read-only; git-ignored artefacts only; `git status --porcelain` before/after)
- report path `…/scratchpad/audits/A3-P4-R1-report.md`
- Frozen candidate FC-P4: `…/scratchpad/audits/FC-P4-manifest.txt` — manifest_sha256 and entry count in its header (filled at freeze; recompute at start and end).

## What's new since FC-P3e2 — verify independently
1. Six cards, disjoint §3 write sets, precode frozen during the wave, then a fix round (rulings `packets/FIX-P4-wave-rulings.md`, Owner round `OD-20260908-10`):
   - Phase 4: `TC-report-coverage-publish-cas` (`server/app/report/{coverage,builder,publisher,router}.py`, migration `0010_tc_report_coverage_publish_cas` — creates `report`, `report_item`, `emerging_direction`, `coverage_window`, custodian `pending_item_ledger`/`backfill_ledger`, rebuilds `first_announced_ledger` for its FK), `TC-backfill-pending-ledger` (`server/app/report/{backfill,pending}.py`, `server/app/tags/rescan.py`, `0013` creates `tag`/`settings`/`rescan_ledger`), `TC-ui-runs-three-states` and `TC-ui-reports-detail` (`web/src/lib/{api,runState,provenance}.ts`, routes, views, Vitest).
   - Phase 6: `TC-scheduler-lease-claim` (`server/app/scheduler/evaluator.py`, `server/app/jobs/{service,lease,router}.py`, `0010_tc_scheduler_lease_claim` **extends** custodian `assignment_lease` with a partial unique index; `0011_merge_phase4_6_heads`), `TC-backup-restore-drill` (`server/app/backup/{snapshot,verify,restore,reconcile}.py`, `tools/backup_cli.py`, `0012`; CLI-only, no router by ruling).
2. Fix round to verify: CR-TC-REPORT-09 (W5C's `report` stand-in vs the real table — 18 setup errors gone), CR-TC-REPORT-08/-04 (W3C: xfail re-pointed to `report.publisher.publish_report`; cosine `ROUND_HALF_UP`), CR-TC-BACKFILL-05 (W4A: `backfill_port` on `build_report`; W4B positive path real), OD-20260908-10 item 1 (W5B: save without analysis allowed with a fixed "not analysed yet" literal, no inferred text, no undeclared column), stale `pending TC-…` xfails re-checked by W3B/W5A/W5C/W4C now that report/scheduler/backup exist.
3. Owner decisions OD-20260908-10 items 2–3 (keep `/save <id>`; linked-chat reminder) — confirm code matches; the sweep-fixture divergence is a recorded CR, not silently overridden.
4. Migrations: one head; force both branch orders from a blank DB (`0010` report and `0010` scheduler branched off `0009` concurrently, merged by `0011`; `0012`, `0013` linear after); `tests/contract/test_schema_matches_entities.py` green; W6B removed two undeclared idempotency columns after that test caught them — confirm none remain; `0002b` tables taken over not recreated.
5. Key oracles to reproduce, not read: coverage windows contiguous/non-overlapping incl. empty periods and after a publisher crash; two concurrent publishers ⇒ CAS, one wins; published report hash unchanged; add→remove→re-add entitles once and is consumed exactly once on publish; three run states ⇒ three distinct strings; provenance labels never collapsed; three offline periods ⇒ exactly one catch-up run; two claimants ⇒ one held lease, stale epoch rejected, raw SQL refused by the index; DST gap/repeat per fixture `j`; WAL-unsafe copy refused (manufactured, not simulated); post-restore `COUNT(telegram outbound)=0` until reconciled through W5C's real dispatcher; restore only inside an operator-opened maintenance window; `new_restore_generation` exceeds every existing generation.
6. Whole-suite no-network run (socket + DNS blocked) again — new transports exist (`tools/backup_cli.py`, report router, jobs router).
7. Whole-diff scan for undeclared writes / lease violations; the workers' ~50 CRs — flag any "declared deviation" that hides a real defect; note E3/E4 NOT_RUN items (real restore drill, UI render review, live calls) stay NOT_RUN.

## Method
Full regression from a clean env (pytest whole workspace, ruff check + format, mypy --strict, `npm --prefix web test`/lint/typecheck), e0 (26/26 expected), verify_cards (13/13 @ the current epoch), OpenAPI validator, alembic one head both orders; per-card claim verification; scope verdicts; report as before.

Reply ONLY: overall verdict, per-card claim verdicts (6), key numbers, new findings count + severities, report path (≤30 lines).

## Freeze record (2026-09-08T01:2xZ)
- FC-P4 manifest_sha256 `04d4215d36c153f41500e06489ed3c84f5b42f3049e19248979cecca51508a2a`, 635 entries (git-tracked + untracked-not-ignored, `.claude/` and `__pycache__` excluded). Epoch `PC10-PIN-P4-20260908`.
- Coordinator's own read-only measurements on the frozen bytes (verify, don't inherit): pytest 1028 passed / 6 xfailed / 0 failed; `ruff check` clean; `ruff format --check` 165 files clean; CI-scope `uv run mypy` Success (64 files); web: 126/126 Vitest, tsc clean, eslint+prettier clean; one Alembic head `0013_tc_backfill_pending_ledger`; verify_cards 13/13, 3730 assertions, 0 violations; e0 26/26; zero `__pycache__` dirs.
- Disclosed: a broader `mypy --strict server worker collector probe` reports 7 errors outside CI's configured scope — `worker/app/adapter/{base,validate}.py` import-untyped for `yaml`/`jsonschema` (known CR-TC-adapter-06: `types-PyYAML`/`types-jsonschema` dev deps live in the shared `pyproject.toml`, a skeleton file no card may edit) and 5 in `server/tests/test_smoke.py` (Phase-0 skeleton test, pre-existing). Judge whether CI scope is the right bar or whether these should block.
- Fix-round manifests re-issued for: adapter (none this round), analysis (010738Z), embedding (004611Z), tgauth (004625Z), saved (054000Z), delivery (004909Z), report (005154Z), backfill (010136Z), ui-runs (005139Z), ui-reports (220500Z), backup (064000Z); older ones marked STALE, retained.
