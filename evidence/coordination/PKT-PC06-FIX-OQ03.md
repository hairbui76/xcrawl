# PKT-PC06-FIX-OQ03 — resolve REQ-OQ03, advance REQ-A5 for the chosen vendor

Dispatched to a new Worker (`worker-WAI`), 2026-09-08. Authority `AUTH-COORD-OQ03` (parent `AUTH-OWNER-20260908-06`, see `packets/OWNER-DECISIONS-20260908-05.md` item 1 and the follow-up Owner answer below). Lease `LEASE-PC06-OQ03` on: `precode/decision-register.md`, `precode/requirements.csv` (the `REQ-OQ03` row only), `contracts/ai/providers.yaml`, `precode/change-control.md`, plus CREATE `precode/owner-decisions-06.md` and a handoff at `evidence/handoffs/PC06-OQ03-handoff.md`. No other file.

## The Owner's decision, verbatim
Asked to choose between (a) single-vendor API (Claude Haiku 4.5 + Sonnet 5), (b) cost-optimized mixed vendors, (c) zero-API-key CLI path, (d) specify directly — the Owner answered: **"sonnet 5 + opus 5 for summary"**. Read literally: Claude Sonnet 5 for the `label` task (and, absent any other instruction, also for `direction_phrasing` — the research fork classified `direction_phrasing` alongside `summary` as needing a strong model; if you think that reading is wrong given how `tasks.yaml` actually describes `direction_phrasing`, flag it, don't silently pick either way), Claude Opus 5 specifically for the `summary` task. Both are `api_key` family (Anthropic), not the `cli_acp` path. This is the Owner's exact answer — do not substitute a different model or vendor.

## Two separate gates — do not conflate them
1. **`REQ-OQ03`** (which provider/model) — resolved by the Owner's answer above. Record it as such: `precode/owner-decisions-06.md` (new, same convention as prior `owner-decisions-0n.md` files), `precode/decision-register.md`, `precode/requirements.csv`'s `REQ-OQ03` row. This unblocks Phase 3's entry gate per `docs/master-plan.md` §3 ("Cổng vào: REQ-OQ03 phải được Owner trả lời") — the gate is that it's *answered*, not that every adapter is enabled.
2. **Enablement** (`provider_config.enabled = true`) is a **separate, harder gate** that this packet does **not** need to close, and must not fake closing. Read `contracts/ai/providers.yaml` §4 (isolation) and §5 (terms_check) in full before writing anything. In particular: §4's `current_status_vi` states plainly that at this stage **every** adapter has `isolation.* = unverified` and therefore `enabled = false`, "no exceptions, since no probe has run (E3 = NOT_RUN)" — but §4's `status_values` also defines a `not_applicable` status specifically for the `api_key` family "for properties the architecture has already eliminated." Determine, from the contract text itself (not from general reasoning), whether any of `ISO-01..05` can honestly carry `not_applicable` for a plain HTTP `api_key` adapter with no tool-calling surface, or whether the "no exceptions" line means literally none can — cite exactly where the text answers this before you decide. Do not resolve this ambiguity in whichever direction is more convenient; if the contract is genuinely silent, say so and leave `enabled: false` with an honest `disabled_reason`.

## Do
1. Record the `REQ-OQ03` resolution (item 1 above).
2. **REQ-A5 for Anthropic**: fetch Anthropic's own current terms of service / usage policy documentation — narrow network grant, read-only, documentation/legal pages only, hosts `anthropic.com`, `www.anthropic.com`, `docs.anthropic.com` (their usage policy, terms of service, and API-specific terms pages). No live API calls to `api.anthropic.com`, no API key used. Determine and record, with URL + verbatim quote + retrieval date (2026-09-08), whether Anthropic's current terms permit this system's actual use case: automated, unattended, at-scale content labeling and summarization of third-party research-related text (arXiv/OpenAlex metadata and similar), stored and reused inside a self-hosted tool. Do not soften an ambiguous or restrictive term into a "yes."
3. Populate two adapter entries in `contracts/ai/providers.yaml` following §2's `adapter_entry_template` exactly: `claude-sonnet-5@<version>` (task_support includes `label`, and `direction_phrasing` per the reading above unless you flag it as wrong) and `claude-opus-5@<version>` (task_support: `summary`). Fill every required field. Set `terms_check` from step 2's finding. Set `isolation.*` per your determination in the "Two separate gates" section — if you conclude `enabled: true` is actually reachable here, justify it explicitly against the contract text, don't default to `false` out of caution alone, but equally don't force `true` to make progress; if it's genuinely still blocked, `enabled: false` with a specific, non-generic `disabled_reason`.
4. Add a `change-control.md` entry in the file's existing format.
5. Run `evidence/tools/e0_check.py` read-only.

If Anthropic's terms are ambiguous or you can't find an authoritative statement after a reasonable search, report that specifically as BLOCKED for REQ-A5 (this does not block the REQ-OQ03 recording in step 1, which stands regardless) — do not guess.

Reply ONLY: status; REQ-OQ03 recorded (y/n); REQ-A5 finding (permitted/not permitted/ambiguous) with citation; the two adapter entries' `enabled` value and why; files changed + hashes; e0 result; any CR (≤25 lines).
