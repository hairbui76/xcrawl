# REVIEW TASK_PACKET PKT-A3-P5-R1 — independent review of the integration wiring wave — FC-P5

- packet_id `PKT-A3-P5-R1` · assignee `auditor-A3` · authority `AUTH-COORD-A3-P5-R1` (parent `AUTH-OWNER-20260908-11`) · lease null (read-only; git-ignored artefacts only; `git status --porcelain` before/after)
- report path `…/scratchpad/audits/A3-P5-R1-report.md`
- Frozen candidate FC-P5: `…/scratchpad/audits/FC-P5-manifest.txt` — hash + count in header (filled at freeze; recompute at start and end).

## Why this wave exists — verify the premise first
`docs/owner-runbook.md` (WS2) found that all 19 cards were built and tested against ports and harnesses, but no runnable application existed: `create_app()` built no services (`auth.login` → 500 outside tests), no owner-bootstrap CLI, storage health not persisted across CLI invocations, alembic only from `server/`, probe `output_dir` relative to the config file, collector/worker `main.py` Phase-0 stubs, no secret/settings service (three `secret.*` ops with no code, no valid place for an API key or webhook secret). Gaps G-1…G-10 in the runbook; packets `WIRING-wave-1.md`.

## What to verify
1. **Composition root (WS, P0-FIX5)**: `server/app/settings.py` + `server/app/wiring.py` build engine → repositories → services → `app.state.*_context` the way each card's tests do — confirm no card's service signature changed and no delimited block in `main.py` moved; `tools/rr_admin.py migrate | bootstrap-owner | status`; alembic from any cwd; `server/tests/test_wiring.py` proves blank DB → migrate → bootstrap → real factory → login 200 → readiness 200 → `/v1/runs` 200 with **no monkeypatching**. Then do it yourself from a shell in a temp dir: `RR_DATABASE_URL=… uv run python tools/rr_admin.py migrate`, `bootstrap-owner`, `uvicorn server.app.main:app`, login with curl — this is the first time anything in this repo is exercised as a process rather than a TestClient.
2. **Persisted storage health (WR)**: `backup_cli maintenance --open` observed by a second process (or STOP+CR if the contract has no entity — verify which it is).
3. **Probe (WX)**: relative `output_dir` rule; `--dry-run` creates nothing (run it from a temp cwd and check).
4. **Collector loop (WC)** and **worker loop (W3A)**: `main.py` refuses without config with a named reason; in-process harness cycles run against real server state; no live X/AI; adapters still `enabled:false` ⇒ the worker loop records the refused attempt and backs off — never a fake success.
5. **Secret/settings service (WAI, 20th card)**: envelope encryption per `secrets.md` §4.1, master key from env only (never default, never logged, canary never leaves); access matrix §4.2 (`UNAUTHORIZED`/`CAPABILITY_DENIED`); per-task credential issued only to the lease holder, ISO-05 negative really executed with `COUNT(task_credential)=0`; `settings` table taken over from `0013` not recreated; `provider_config.enabled=true` requires `terms_check_at` (CHECK); one head `0014`; `test_schema_matches_entities.py` still 0 differences both ways.
6. Regression: pytest, ruff/format, mypy (87+ files), web, e0 (27/27), verify_cards (13/13 over **20** cards), OpenAPI, one head both branch orders, socket/DNS-blocked whole suite (new processes: rr_admin, collector/worker mains).
7. Whole-diff scan; the workers' CRs; anything still unrunnable for the Owner (list it plainly — this wave's purpose is "the Owner can test it").

Reply ONLY: overall verdict, per-packet verdicts (7), "can the Owner run it end to end?" yes/no with what still cannot, key numbers, new findings count + severities, report path (≤30 lines).

## Freeze record (2026-09-09T07:57Z)
- FC-P5 manifest: see header of `…/scratchpad/audits/FC-P5-manifest.txt` (hash printed at freeze; recompute). Epoch `PC10-PIN-P5c-20260909` over 20 cards.
- Coordinator read-only measurements on the frozen bytes: pytest 1161 passed / 3 xfailed / 0 failed; ruff check + format (187 files) clean; `uv run mypy` Success 101 files; web 126/126 + tsc + eslint/prettier; one Alembic head `0015_tc_storage_maintenance_window`; verify_cards 13/13 over 20 cards (3962 assertions, 0 violations); e0 27/27; gen-check clean; 0 `__pycache__`.
- Packets landed (all released): WS P0-FIX5/6/7 (composition root, rr_admin, regen, intent_id key, secret/settings contexts, cryptography dep); WR STORAGE-FIX4 (0015 maintenance_window, from_engine); WX PROBE-FIX2; W3A ADAPTER-FIX2/3 (worker loop); WC COLLECTOR-FIX2/3/4 (collector loop, client 409 branch); W6A SCHED-FIX1/2 (alert port connection, claim/stop-report shapes); W3B ANALYSIS-FIX4 (analysis_key); W6B BACKUP-FIX3 (owner resolve, from_engine, two-invocation restore); W3n PC02-FIX16/17/18/19 (AMD-ENT-maintenance-01 PROVISIONAL, purge 37/22/2 propagated); WP PC10-FIX28/29/30 (20th card; epochs P5 superseded-unreleased, P5b, P5c); WAI TC-secret-settings-service + FIX1 (AES-256-GCM real cipher); W6n E0-18 count leg + E0-21 ast + verify_cards self-test fix.
- Coordinator rulings to judge: WS's master-key split (unset ⇒ run with store=None, REQ-D51; malformed ⇒ refuse); fixture-is-canonical for report_stop's shape; AMD-ENT-maintenance-01 as a Coordinator technical amendment (Owner not yet asked); no version bumps on the nine count-propagated artefacts (DEV-PC02-FIX17-01).
- Owner-facing question the report must answer: can the Owner now run it end to end from `docs/owner-runbook.md` (still WS's §§3–4 text until WS2 re-verifies post-commit)? Name every remaining gap (known: report_context unwired — CR-P0-07, tag service has no card — CR-TC-SCHED-06; E3/E4 NOT_RUN everywhere).
