# Coordinator rulings — post-A3-P4-R1 fix round — 2026-09-08

Report: `…/scratchpad/audits/A3-P4-R1-report.md` (PASS with one scoping condition; 3 findings). Authority `AUTH-OWNER-20260908-11`. Findings OPEN → FIX_PROPOSED; A3-P4-R2 verifies on FC-P4 epoch 2. PC09-P4 and packaging gates move to the R2 report.

| Finding | Ruling | Owner |
| --- | --- | --- |
| F-A3-P4-01 MEDIUM — `publisher._write_first_announcements` writes first-announcement rows for items still `summary_state='pending'`, so a late discovery returns as `prior_reference` (fixture `reporting/e`, REQ-D29, I07). Declared by W4B (CR-TC-BACKFILL-09, strict xfail) but unruled and absent from W4A's handoff while W4A lists REQ-D29/I07 as covered. | W4A fixes the condition (announce only items whose summary is final per `contracts/reporting` / I07; cite the clause), adds a test with a pending item that is not announced and is announced once when its summary lands; handoff addendum names CR-TC-BACKFILL-09 explicitly. W4B then flips its strict xfail (gated on W4A's addendum). | W4A → W4B |
| F-A3-P4-02 MEDIUM — mypy scope `files = ["server/app"]` (CI + ADR-0011 "lõi server") leaves 23 production files in `worker/app`, `collector/app`, `probe/` type-checked by nothing; the 7 broader-scope errors are missing stubs + a Phase-0 smoke test. | Not blocking CI (correct bar today), but fix the bar: WS (skeleton) widens mypy `files` to include `worker/app`, `collector/app`, `probe`, adds `types-PyYAML`/`types-jsonschema` dev deps (closes CR-TC-adapter-06), fixes or excludes `server/tests/test_smoke.py` typing, keeps `--strict`, verifies clean, updates the CI step name/Makefile. W1n amends ADR-0011 (type-check scope = every Python production tree; also fixes CR-PC00-35 "tools/ not created" and adds `__pycache__/` to .gitignore via WS) — precode edit batched before the re-pin. | WS; W1n |
| F-A3-P4-03 LOW — `test_fixture_i_preserved_published_report_items` (identity-merge card) still xfails with prose "needs report/report_item tables"; both exist now. 4th recurrence of stale-xfail reasons. | WM makes it real against W4A's tables (seed via `report.publisher.publish_report`, merge, assert published items preserved). Standing check: PC09-P4 adds an e0 rule flagging any `xfail` whose reason names a card/table that exists (E0-21), mutation-tested. | WM; W6n |
| Order | W4A ∥ WS ∥ W1n (precode) ∥ WM → W4B (gated on W4A) → WP re-pin P4b (ADR-0011 moved) → Coordinator re-verify → freeze FC-P4 e2 → A3-P4-R2 (scoped) → W6n PC09-P4 + W1n FIX31 (gates moved to R2) → commit+push → final report. | |
