# Coordinator rulings — post-A3-R1 fix wave (Phase 0/1) — 2026-09-07T11:00Z

Report: `…/scratchpad/audits/A3-R1-report.md`. Authority: AUTH-OWNER-20260907-03. Findings stay OPEN → FIX_PROPOSED; A3-R2 verifies on FC-P1 epoch 2.

| Finding | Ruling | Owner |
| --- | --- | --- |
| F-A3R1-02 HIGH (credential columns not in contract) | **Contract completed through change control (CR-TC-AUTH-02, CR-TC-AUTH-03 ACCEPTED as technical amendments consistent with the Owner-accepted `contracts/ops/secrets.md`):** `ENT-owner` gains `password_hash` (text, nullable until bootstrap; Argon2id encoded string), `password_updated_at` (RFC3339 ms, nullable), `failed_login_count` (int, NOT NULL default 0), `locked_until` (RFC3339 ms, nullable). Rationale row cites secrets.md §owner auth (lockout 5/15 min) and D05. entities.yaml is CONTRACT_READY → this is an amendment (`AMD-ENT-owner-01`, decision_ref CR-TC-AUTH-02/03, ratification: Coordinator technical, disclosed to Owner in the next report; Owner may object). Regenerate `shared/rr_contracts` afterwards. Permanent check: a server test `tests/contract/test_schema_matches_entities.py` asserting every column of every table after `alembic upgrade head` resolves to an entities.yaml field (and every NOT NULL/UNIQUE/CHECK the entity declares is present). | W3n (entities) → WS (regen) → WA (test + use) |
| F-A3R1-01 HIGH (two `CREATE TABLE owner`) | `0002_base_entities` is the sole `CREATE TABLE owner` with the full contract constraint set incl. the four new columns; the auth revision drops its CREATE and depends on `0002_base_entities` (linear after it). Test asserts the constraint set after `upgrade head` from a blank DB and from the reverse branch order (if branches remain). | WM (base revision), WA (auth revision + test) |
| F-A3R1-06 MEDIUM (lockout process-local) | Persist `failed_login_count` / `locked_until` on the owner row (fields above) inside the login transaction; test survives a factory restart. | WA |
| F-A3R1-08 MEDIUM (SG-DENY minority) | `test_denied_edges.py` exercises every one of the 36 sweep events that is reachable at the HTTP/service layer (all 11 UNAUTHORIZED, all 8 FORBIDDEN_EDGE via the service-layer `_require_edge`/router guards); the 15 CAPABILITY_DENIED (process/network capability) are asserted as `NOT_TESTABLE_AT_THIS_LAYER` with the enforcing mechanism named per modules.yaml, never counted as passed. | WA |
| F-A3R1-12 LOW (plaintext token dict) | Tokens compared with `hmac.compare_digest`; stored as hashes per secrets.md token section; lookup by hash. | WA |
| F-A3R1-04 MEDIUM (five foreign tables in identity revision) | Move `saved_snapshot`, `analysis`, `saved_item`, `work_label`, `first_announced_ledger` DDL out of the identity revision into `0002b_shared_move_set_tables` (owner note: MOD-data-store on behalf of MOD-saved/analysis/report; header lists the owning cards that will take them over; CR to those cards). | WM |
| F-A3R1-14 LOW (`owner.created_at` no CHECK) | Add the timestamp CHECK in the base revision. | WM |
| F-A3R1-05 MEDIUM (owner scoping / receipt link FKs) | `checkpoint.owner_id`, `ingest_receipt.owner_id` → `REFERENCES owner(id)`; `post.ingest_receipt_id` FK enforced (coordinated: the receipt table must exist before `post` FK — order the revisions or use a deferred-creation ALTER in SQLite via table rebuild in the ingest revision; document the choice). | WI (with WM for `post` in base revision) |
| F-A3R1-10 LOW (`ingest/__init__.py`) | Add. | WI |
| F-A3R1-09 LOW (two read-set fixtures unexercised) | Each card checks its §2/§8 fixture list: exercise or record `NOT_RUN` with reason in the handoff addendum. | WI, WM, WA, WR |
| F-A3R1-11 LOW (guard and write gate unwired on the factory) | `install_storage(app, guard)` hook wired in `create_app()` include block; ingest's write gate calls `app.state.storage_guard`; tests on the bare factory. | WR (install), WI (call) |
| F-A3R1-13 LOW (include delimiters) | Every include block in `server/app/main.py` inside its own `# >>> card / # <<< card` delimiters. | WR (its block), others verify theirs |
| F-A3R1-07 MEDIUM (`db/faults.py` outside write sets) | Adopted into the Phase 0 skeleton (skeleton owner records it in an addendum with hash; the dispatch note was the Coordinator's defect). | WS |
| F-A3R1-15 LOW (README `uv sync` form) | `uv sync --all-packages`. | WS |
| F-A3R1-03 MEDIUM (index.json) | PC09 registers the four E1 runs + A3 records. | W6n (PKT-PC09-P1) |
| Order | W3n ∥ WM ∥ WI ∥ WR ∥ WS(F-07,F-15) → WS regen (gate: W3n) → WA (gate: regen) → WP re-pin (entities changed) → freeze FC-P1 e2 → A3-R2 → W6n PC09 → commit. | |
