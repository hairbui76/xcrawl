# REVIEW TASK_PACKET PKT-A1-R3 — Verification of F-A1R2-01…06 fixes + independent audit of PC05, PC06, PC07, PC08 (FC-W3 epoch 3)

- packet_id: `PKT-A1-R3` · assignee: `auditor-A1` · authority `AUTH-COORD-A1-R3` · lease null (read-only)
- report path: `…/scratchpad/audits/A1-R3-report.md` · helpers under `…/scratchpad/a1/` · PYTHONDONTWRITEBYTECODE=1 · no repo writes, no git mutation, no network
- Frozen candidate FC-W3 epoch 3 manifest: `…/scratchpad/audits/FC-W3-manifest.txt` (manifest_sha256 recorded in the file header). Recompute at start and end. Ignore only files under `acceptance/scenarios.yaml`, `acceptance/traceability.csv`, `evidence/{manifest.schema.json,index.json,tools,runs}`, `precode/{gates.yaml,review.md,change-control.md,README.md}`, `agent-tasks/` if they appear (PC09/PC10 in progress, out of scope).
- completion_ceiling: DRAFT_FOR_REVIEW. audit_route rationale: wire/auth/secrets/backup/AI-isolation contracts (HIGH), unverified fixes to MAJOR findings.

## Part A — Verification verdicts for F-A1R2-01 … F-A1R2-06
Inputs: `A1-R2-report.md`; rulings `…/scratchpad/packets/FIX3-rulings.md`; fix addenda: PC00-FIX2 (PC00-handoff), PC01-FIX3/FIX4 (PC01-handoff), PC02-FIX3 (PC02-handoff), PC03-FIX1 (PC03-handoff), PC04-FIX1 (PC04-handoff), PC05-FIX1 (PC05-handoff). Re-run each original reproduction; verdict VERIFIED | NOT_VERIFIED | PARTIAL with evidence. Also re-run the epoch-1/2 sweeps (entity↔owner set diff, fixture-actor-edge over ALL fixture directories, REQ/op/error/entity reference resolution over ALL contract files, header fields, forbidden strings, claim ceiling) to detect regressions.

## Part B — Independent audit of PC05–PC08
Packets: `…/scratchpad/packets/PC05-packet.md`, `PC06-packet.md`, `PC07-packet.md`, `PC08-packet.md` and the Coordinator dispatch notes summarised here: PC05 — security schemes PROVISIONAL (cookie+CSRF UI, bearer collector/worker, Telegram secret header), CR-PC03-04, R-01 in endpoint descriptions; PC06 — task_type per entities.yaml, retry budget consistent with retry-policy.yaml (one auto re-run from unknown_attempt), analysis key per ADR-0008, B13 credential ops; PC07 — unlink is an app action (CR-PC01-02), delivery cites PC03 delivery.yaml, saved-snapshot schema matches entities; PC08 — CR-PC02-07 (I15 data part), purge_all exclusions OWNER_DECISION_REQUIRED, converge on PC05 schemes.
Method as R1/R2, plus per package:
1. **PC05**: every ports.yaml `transport: http` op ↔ exactly one operationId (re-run); no internal op exposed; every `$ref` resolves; ErrorEnvelope = errors.yaml; mutations carry idempotency/lease fields and the error responses the packet lists; `CSRF_REJECTED` mapped on owner mutations; `backup.*` on backupOperatorToken; fixture bodies validate; checkpoint semantics (R-01) visible; probe protocol has go/no-go, stop conditions, Owner gate, NOT_RUN; arXiv/OpenAlex requirements KC without fabricated URLs (judge CR-PC05-03); no OpenAPI validator installed — state the limitation honestly.
2. **PC06**: task schemas separate; input carries source ids/hashes/evidence level; output schema `additionalProperties:false`, `oneOf` by task; analysis key = ADR-0008 = entities.yaml; retry budget does not multiply (D43) and matches retry-policy.yaml numbers; fallback allowlist owner-configured; usage `unknown` never 0 (I14); CLI/ACP isolation verification method and "disabled if unverifiable" (B13); zero-API-key path covers all three tasks (AC-16); adversarial fixtures (a)–(k): positives validate, negatives fail at the stated check — re-run; grounding: three statement kinds (B16), comparator unknown, no AI selection/density; probe protocol NOT_RUN; no provider terms asserted as fact (A5 KC).
3. **PC07**: every spec §4 screen mapped; each action → one ports.yaml op with auth scope; three run states with exact labels and the forbidden label absent (spec §8.3); resume location (B10); commands: save/run_now/status semantics incl. run_now never overriding needs_user; B09 exception; ingress auth/replay; link generation; unlink app action; delivery.md consistent with PC03 delivery.yaml states and retry-policy numbers (no restated numbers); unknown never shown as sent/failed; outbox created at publish (B01); saved-snapshot schema vs entities `saved_item`/`saved_snapshot` (content_hash, canonical JSON rule) — re-validate fixtures; fixture (h) outbound calls 0; (a) no resend of unknown part; I08/I09/I13 positive+counterexample.
4. **PC08**: single account/no signup/no self-service reset; session + CSRF mechanism = PC05 schemes; token scopes = capabilities.yaml; secret store/key-at-rest; per-task credential (B13) = ports.yaml secret.* ops and PC03 `lease_ttl_analysis`; Chrome loopback/profile excluded from backup; URL fetch/SSRF/redirect rules with oracles; content rendering; backup = Online Backup API / VACUUM INTO with cited SQLite URLs (these two URLs ARE in the plan §3.1), manifest fields vs entities `backup_manifest`/`restore_record` (post PC02-FIX3), restore drill with dispatch lock until reconciliation (I15, RESTORE_UNVERIFIED), RPO/RTO PROVISIONAL with rationale; recovery fixtures (a)–(j) incl. fixture (i) `UNAUTHORIZED` (ruling CR-PC08-04) and fixture actors.
5. Cross-package: security scheme convergence PC05↔PC07↔PC08; error codes used exist; scenario IDs SC33–SC44+ unique across packages (PC06/PC07 took SC45+ — check collisions); every fixture directory README lists all files; headers; language; claim ceiling; no TBD/CLOSED/ACCEPTED.
6. Evidence honesty of all four handoffs (+ FIX addenda); re-run what you can.

## Report
Same structure as R1/R2; findings `F-A1R3-nn`; Part A table; per-package verdicts PC05–PC08 and epoch-3 verdicts for PC00–PC04 (regression sweep); limitations; ceiling. Reply to the Coordinator ONLY: Part A verdicts one line each; per-package verdicts; finding counts by severity; five most important findings one line each; report path (≤30 lines).
