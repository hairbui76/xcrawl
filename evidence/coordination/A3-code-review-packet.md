# REVIEW TASK_PACKET PKT-A3-R1 — Independent code review of Phase 0 + Phase 1 (frozen candidate FC-P1)

- packet_id: `PKT-A3-R1` · assignee: `auditor-A3` (fresh principal; authored nothing) · authority `AUTH-COORD-A3-R1` (parent `AUTH-OWNER-20260907-03`) · lease null
- report path: `…/scratchpad/audits/A3-R1-report.md` · helpers under `…/scratchpad/a3/` · PYTHONDONTWRITEBYTECODE=1 · **no repo writes**; you MAY run the test suites and linters read-only (they write only under `.venv`, `node_modules`, `.pytest_cache` which are git-ignored — confirm with `git status` before and after; if anything untracked appears outside those, report it and do not clean it up)
- Frozen candidate manifest: `…/scratchpad/audits/FC-P1-manifest.txt`. Recompute at start and end.

## Inputs
`agent_profile/auditor.md`, `protocol.md`; `docs/master-plan.md` §2–§3 (Phase 0/1 definition of done); `precode/adr/ADR-0011…`; the four cards `agent-tasks/TC-ingest-idempotent-ack-lost.md`, `TC-canonical-identity-merge.md`, `TC-owner-auth-session.md`, `TC-storage-write-blocked-readiness.md` (each §6–§13 is the oracle); the contracts they cite; the handoffs `evidence/handoffs/P0-skeleton-handoff.md` and `<card>-handoff.md`; evidence manifests under `evidence/runs/`.

## Method
1. **Reproduce the evidence**: `uv sync`, `uv run pytest` (whole workspace), `uv run ruff check`, `uv run mypy` (per ADR scope), `npm ci && npm test && npm run lint` in `web/`, `evidence/tools/e0_check.py` read-only, `evidence/tools/verify_cards.py`, the OpenAPI validator. Compare with the handoffs' claims check-for-check; any skipped/xfail test for a P0 scenario is a finding.
2. **Card compliance**, per card: every §3 path exists and nothing outside §3 (+ migration + handoff + manifest) was written by that card; every §4 operation implemented with the generated models; §5 denied edges impossible (e.g. collector token cannot reach `save.create` — write a test if none exists and run it read-only in your scratch); §6 transactions are single commits with the stated commit point, replay-safe (run the replay fixture yourself); §7 every error code mapped and raised from generated constants; §8 every listed test present and passing; §9 claim not exceeded in the handoff; §10 stop conditions honoured (no contract edited — diff `contracts/`, `acceptance/`, `precode/` against the pre-phase commit `da886f8`, expecting only PC00's record files changed).
3. **Correctness review** (read the code): SQL transaction boundaries vs TXN-ingest-batch / TXN-checkpoint-only / TXN-identity-merge / auth session + CSRF flow / storage `write_blocked` + independent readiness channel; idempotency (same key+payload → stored receipt; same key+different payload → IDEMPOTENCY_CONFLICT; no partial writes); UNIQUE/partial-index/CHECK constraints from entities.yaml actually present in the Alembic migrations (grep the migration DDL); Argon2id parameters per secrets.md; cookie flags (HttpOnly, SameSite, Secure) and CSRF double-submit per PC05/PC08; lockout counters; no secret in logs; `owner_id` on every table; generated-code rule (GENERATED_FROM hashes match contracts; no hand edits).
4. **Skeleton**: layout = cards' §3; CI workflows run the four jobs + OpenAPI validator; `.gitignore` covers `.venv`, `node_modules`, `*.db`; lockfiles present; no secrets.
5. **Regression**: contracts/acceptance/agent-tasks/precode unchanged except the PC00 decision record (diff vs `da886f8`).

## Report
Findings `F-A3R1-nn` (severity, file:line, condition, expected/observed, impact, remediation constraint — no patches); per-card verdicts (PASS / FAIL / BLOCKED) and per-card claim verdict (may `IMPLEMENTATION_VERIFIED` stand?); skeleton verdict; evidence-reproduction table; overall verdict; residual list. Reply to the Coordinator ONLY: overall; per-card verdicts + claim verdicts; finding counts; top five findings one line each; report path (≤30 lines).
