# Coordinator rulings — post-A3-P5-R1 fix round — 2026-09-09

Report: `…/scratchpad/audits/A3-P5-R1-report.md` (PASS with findings; first process-level audit). Authority `AUTH-OWNER-20260908-11`. Findings OPEN → FIX_PROPOSED; A3-P5-R2 verifies on FC-P5 epoch 2. PC09-P5 and packaging gates move to the R2 report. Precode/contracts frozen.

| Finding | Ruling | Owner |
| --- | --- | --- |
| F-A3-P5-01 M — runbook's `backup_cli maintenance --open` crashes (`MaintenanceWindowRequired` traceback): `reason` became mandatory in the guard, `--reason` stayed optional, and the error bypasses the JSON envelope | `--reason` required on `--open` (argparse `required=True`, choices from `MAINTENANCE_REASONS`); every guard exception maps to the envelope + documented exit code, never a traceback; test: missing `--reason` ⇒ envelope + exit 2. | W6B |
| F-A3-P5-02 M — `wiring.py:476` tells the Owner the connector is blocked by "REQ-A6 facts still PLACEHOLDER_KC" (false since Phase 2) | Correct the message to the real blockers (SG-DOC: no API host/endpoint contracted; SG-LIVE: E3 forbidden) and read the status from `contracts/retry-policy.yaml` at run time, not a literal; `rr_admin status` must print the same. | WS |
| F-A3-P5-03 M — `/v1/reports` → 500 `INTERNAL` "unexpected" for a disclosed gap (`report_context` unwired, CR-P0-07) | The report router answers a **declared** error when its context is absent: use the code `contracts/http/openapi.yaml` declares for `report.list`/`report.get` unavailability (503 with a named reason if declared; else the closest declared code — cite it; never invent one); reason names CR-P0-07. Wiring sets `report_context = None` explicitly with the reason in `rr_admin status`. | W4A (router) + WS (status line) |
| F-A3-P5-04 L — 3 of 6 entry points self-bootstrap `sys.path`; `backup_cli.py`, collector/worker mains die by path with a bare `ModuleNotFoundError` | Skeleton-level fix: `[project.scripts]` console entries (`rr-admin`, `rr-backup`, `rr-collector`, `rr-worker`, `rr-probe`) in the workspace `pyproject.toml` so `uv run rr-…` works from any cwd; remove the ad-hoc `sys.path` bootstraps once the scripts exist (each owner's file: WX probe, W6B backup_cli, WC/W3A mains — WS edits only pyproject/Makefile/README and files a one-line CR per owner, or, if the bootstrap lines are guarded and harmless, leave them and just document `uv run rr-…` as the supported path). | WS (+ owners if removal is chosen) |
| Runbook §9.4 still says G-3 open and carries the crashing command | WS2 re-verifies post-commit (gated packet already armed, LEASE-DOCS-e3 at the commit). | WS2 |
| `AMD-ENT-maintenance-01` not yet put to the Owner | Asked in the final report of this round (ratify / object). | Coordinator |
| Order | W6B ∥ WS ∥ W4A → Coordinator re-verify (incl. `uvicorn` + curl `/v1/reports`, `backup_cli maintenance --open` without `--reason`) → freeze FC-P5 e2 → A3-P5-R2 (scoped) → W6n PC09-P5 + W1n FIX33 (gates → R2) → commit `feat: integration wiring…` → WS2 runbook-2 → final report. | |
