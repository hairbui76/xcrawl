# Coordinator rulings — FIX6 wave (after AUDIT_REPORT PKT-A2-R1 on FC-W4 epoch 4) — 2026-09-07T01:20Z

Report: `…/scratchpad/audits/A2-R1-report.md`. Findings OPEN → FIX_PROPOSED; verification by A2 (PKT-A2-R2) on FC-W4 epoch 5.

| Finding | Ruling | Owner |
| --- | --- | --- |
| F-A2R1-01 MAJOR (9 non-existent operation ids in `event_order` prose of 8 scenarios; SC12 cites rejected `post.mark_source_deleted`) | Sweep every scenario's `event_order`/prose for `<domain>.<verb_noun>` tokens; each must exist in ports.yaml or be rewritten (SC12: source deletion arrives as ingest item field `source_deleted_observed_at` via `ingest.submit_batch`, per earlier ruling). Add E0 check `E0-04b-prose-op-tokens` scanning prose strings in contracts/ and acceptance/ for operation-shaped tokens and error-code-shaped tokens (SCREAMING_SNAKE with a known prefix) and resolving them against ports.yaml / errors.yaml. | W6 |
| F-A2R1-02 MAJOR (review.md headline numbers contradict artefacts) | Every number in review.md is derived from the artefacts by the E0 tool or a script and cited with its source; reconcile transitions (67), fixtures (count from disk), READY/BLOCKED (table = headline), DoR (table = headline). | W6 |
| F-A2R1-03 MEDIUM (pin epoch stale in README/review) | `PC10-PIN-FCW4b-…` will change again after this wave; W7 re-pins last (epoch `PC10-PIN-FCW4c-20260907`) and updates precode/README.md; W6 writes review.md to cite "the epoch recorded in agent-tasks/README.md" by reference and the value at its run time. | W6, W7 |
| F-A2R1-04 MEDIUM (15 CRs without status) | review.md §8.2 lists all 75 CRs with status. Coordinator dispositions for the two material ones: **CR-PC01-09 APPROVED** — `UNAUTHORIZED_COMMAND` and `RESTORE_UNVERIFIED` (plan §10 codes) are valid expected codes for denied cases outside the four-row R5-01 table when the spec names them; boundary README/modules.yaml wording changes from "awaiting approval" to "approved (Coordinator, A2-R1)". **CR-PC02-18 ACCEPTED_AS_LIMITATION** — the R4-01 column gate applies to `rows[]` fixtures; the eight prose-form originals (collection a–h) and recovery a–j remain disclosed as `NOT_APPLICABLE_FREEFORM` with the file list named in review.md (this also discharges F-A2R1-08). | W6 (status rows), W2 (wording in modules.yaml/boundary README) |
| F-A2R1-05 MEDIUM (NC-28 / sweep seq 26 name `worker.claim_assignment` on scheduler→collector, wrong direction) | The scheduler never calls the collector; the forbidden edge is "scheduler pushes an assignment to the collector". Case and event use `operation: null`, `event_type: in_process_call`, `attempted_action_vi`, expected `FORBIDDEN_EDGE`. E0-10b additionally checks that a named operation's owner is the callee of the edge. | W2 (case/event), W6 (E0 check) |
| F-A2R1-06 LOW (`SAVE_ALREADY_EXISTS` unregistered) | Use `CONFLICT` (registered). entities.yaml prose → `CONFLICT`; `save.create.error_codes` += `CONFLICT`. | W3 (entities), W2 (ports) |
| F-A2R1-07 LOW (I16/I17 in use without decision record) | Register I16 and I17 in precode/decision-register.md as PROVISIONAL data invariants with source refs (from entities.yaml/invariants.md text), cite in baseline.json anchors. | W1 |
| F-A2R1-09 LOW (3 operations without scenario_refs, 2 without error codes) | Fill in ports.yaml (scenario refs from the catalogue; error codes at least `UNAUTHORIZED`, `VALIDATION_ERROR`, `INTERNAL` for mutations). review.md DoR item 5 discloses the fix. | W2 |
| F-A2R1-10 LOW (polarity check treats `mixed` as both poles) | `mixed` counts for neither pole; each invariant needs one scenario declared `positive` and one `negative`; where missing, split or add a scenario (SC54+ allowed; anchor via a one-line PC00 addendum by W1 after W6 reports the IDs — or W6 declares polarity on existing fixtures if the evidence supports it). | W6 (+W1 if new IDs) |
| F-A2R1-11 INFO | Document in evidence/tools/README.md that `files_scanned` includes the run's own output on re-runs; exclude `evidence/runs/` from the scan count. | W6 |
