# Integration wiring wave 1 — make the stack runnable for the Owner — 2026-09-08

Trigger: `docs/owner-runbook.md` (WS2, PKT-DOCS-OWNER-RUNBOOK) found that the cards were built and tested against ports and harnesses, but no composition root exists: `create_app()` builds no services, `RR_DATABASE_URL` is read only by Alembic, login returns 500 outside tests. The Owner cannot test anything end-to-end until this is fixed. Authority: Owner instruction "next I will test by myself" under `AUTH-OWNER-20260908-11`. Common rules as `PHASE3-5-card-dispatch.md`; precode/contracts frozen (defects → CR); handoff path included in every lease (CR-PC00-36).

| Gap | Packet | Owner | Lease |
| --- | --- | --- | --- |
| G-2 no composition root; G-1 no owner-bootstrap CLI; G-4 alembic only from `server/` | PKT-P0-FIX5 | WS | `server/app/main.py` (the factory only — every card's delimited block stays byte-identical), NEW `server/app/settings.py` (env-driven: `RR_DATABASE_URL`, `RR_DATA_DIR`, timezone, provider config path), NEW `server/app/wiring.py` (builds engine → repositories → services → `app.state.*_context` exactly as the cards' tests do; read each card's test fixtures to mirror them), NEW `tools/rr_admin.py` (`bootstrap-owner`, `migrate` = alembic upgrade head from any cwd, `status`), root `alembic.ini` or `make migrate`, `Makefile`, root `README.md` §2, `docs/owner-runbook.md` §§3–4 corrections, P0 handoff addendum, tests `server/tests/test_wiring.py` (blank DB → migrate → bootstrap → login 200 → readiness 200 through the real factory, no test-side monkeypatching). |
| G-3 `storage.health` lives only in process memory → `backup_cli maintenance --open` cannot survive to the next invocation | PKT-TC-STORAGE-FIX3 | WR | `server/app/storage/{guard,health}.py`, its tests, handoff, manifest — persist the health state per `contracts/state/storage.yaml`/`entities.yaml` (if an entity/table for it exists use it; if none exists, STOP with a CR — do not add an undeclared table; `test_schema_matches_entities.py` will catch it). |
| G-5 probe `output_dir` resolves against the config file's dir, not cwd; a `--dry-run` wrote `probe/evidence/runs/SP1-x-feasibility/probe.log` | PKT-TC-PROBE-FIX2 | WX | `probe/x_feasibility/{config,run_probe}.py`, its tests, `probe/README.md`, handoff, manifest — resolve relative to cwd (or make it explicit and documented), never write outside the configured dir on `--dry-run`. The stray log is deleted by the Coordinator (generated artefact). |
| G-7 collector/worker `main.py` are Phase-0 stubs | wave 2 (WC: collector loop against the real `worker.*` endpoints from W6A; W3B/W3A: analysis worker loop) | | after wave 1 lands |
| G-6 no `MOD-secret-service`/`MOD-settings-service` (three `secret.*` ops have no code; no valid place for an API key or the webhook secret) | wave 2 (WP writes card `TC-secret-settings-service` from `contracts/ops/secrets.md` + ports; a worker implements) | | after wave 1 |
| G-8/G-9/G-10 | not code gaps (isolation evidence, Telegram facts, operator token source) — already on the Owner list / CRs | | |

Order: WS ∥ WR ∥ WX → Coordinator re-verify → commit → wave 2 → freeze FC-P5 → A3 → PC09 → packaging → commit → runbook re-verified by WS2.

## Wave 2 dispatched in parallel (2026-09-08) — disjoint from wave 1
| Gap | Packet | Owner | Lease |
| --- | --- | --- | --- |
| G-6 no secret/settings service | PKT-PC10-FIX28 — write card `agent-tasks/TC-secret-settings-service.md` (20th card) from `contracts/ops/secrets.md`, `contracts/ports.yaml` `secret.*`/`settings.*`, `contracts/data/entities.yaml` (provider_config, secret refs), `contracts/ai/providers.yaml` §5; README card index + graph; pin at the current epoch; a worker implements after the card exists | WP → (implementer: WAI, `server/app/secret/`, `server/app/settings_service/` per card §3) | agent-tasks/ (not under the precode freeze; re-pin follows) |
| G-7a collector `main.py` stub | PKT-TC-COLLECTOR-FIX2 — real collector process loop: `collector/app/main.py` + `collector/app/loop.py`: register → claim (`worker.claim_assignment`) → heartbeat → run `client`/`reader`/`batcher` → `ingest.submit_batch`/`commit_checkpoint` → `worker.report_stop`/release, against W6A's real `jobs` router + W?'s ingest router through an in-process TestClient harness in tests (compose the app the way the cards' tests do); config via env (`RR_SERVER_URL`, `RR_COLLECTOR_TOKEN`, Chrome profile path); no live X (fake source port) | WC | collector/app/{main,loop}.py, its tests, handoff, manifest |
| G-7b worker `main.py` stub | PKT-TC-ADAPTER-FIX2 — real analysis worker loop: `worker/app/main.py` + `worker/app/loop.py`: `analysis.claim_task` → `get_task_input` → `secret.issue_task_credential` (port; refuses while G-6 absent — xfail strict/run) → adapter dispatch (still `enabled:false` ⇒ refused, loop records `unknown_attempt`/backoff correctly) → `submit_result`/`heartbeat`; in-process harness; no live AI | W3A | worker/app/{main,loop}.py, its tests, handoff, manifest |

## Ruling 2026-09-08 — G-3 needs a contract amendment (AMD-ENT-maintenance-01)
WR stopped correctly at `SG-EDGE`: `contracts/state/storage.yaml` T-ST-03 requires "write a maintenance-window row" but `contracts/data/entities.yaml` declares no such entity (CR-TC-storage-06, same root as CR-TC-storage-04). Ruling (same shape as AMD-ENT-owner-01, F-A3R1-02): **Coordinator technical amendment** `AMD-ENT-maintenance-01` — add entity `maintenance_window` with the seven fields WR derived from T-ST-03/T-ST-04/T-ST-09 (opened_at, opened_by principal, reason, closed_at, closed_by, storage_health_at_open, restore_record_id nullable FK) + partial unique index "at most one open window"; purge set: retained; ratification: Coordinator technical, disclosed to Owner in the next report, Owner may object. Order: W3n (entities.yaml + change-control + register; version bump; sole precode/contract writer) → WR implements persisted health/maintenance on it (gated on W3n's addendum) → WP re-pin (byte predicate on entities.yaml) → freeze. entities.yaml is not a generator source (CR-P0-06), so no regen.
Also: `tools/backup_cli.py:187` `_resolve_owner` raises `BackupError.__new__(...)` → AttributeError instead of an error envelope → W6B; `server/app/wiring.py:153` mypy export error → WS (in flight).
