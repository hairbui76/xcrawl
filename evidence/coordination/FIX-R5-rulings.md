# Coordinator rulings — post-A2-R5 wave (FC-W4 epoch 8 → epoch 9) — 2026-09-07T05:35Z

Report: `…/scratchpad/audits/A2-R5-report.md`. Authority for all packets: AUTH-OWNER-20260907-02 (OD-20260907-01).

| Finding | Ruling | Owner |
| --- | --- | --- |
| F-A2R5-01 MAJOR (purge scope not propagated) | The ratified `data.purge_all` semantics (delete research data: post, work, post_work, work_label, work_version, identity_alias, identity_conflict, identity_merge_audit, analysis*, tag_vector, embedding_generation, report, report_item, emerging_direction, coverage_window, pending_item_ledger, backfill_ledger, first_announced_ledger, saved_item, saved_snapshot, delivery*, outbox_intent, run, checkpoint, ingest_receipt, assignment*, telegram_update_log, telegram_link_attempt; **retain** owner, session, secret_ref, task_credential, secret_audit, provider_config, provider_test_result, settings, schedule_occurrence, telegram_link, telegram_link_code, source_connection, worker_registration, schema_migration, backup_snapshot, backup_manifest, restore_record, data_deletion_audit, purge_challenge — use entities.yaml TXN-purge-all's 20-table retained list as the authoritative enumeration; backups untouched; confirmation dialog states that purged data persists in backups) must appear identically in: contracts/ops/secrets.md, backup-restore.md (W2); acceptance/fixtures/recovery/l-* full assertion of all retained tables + README (W2); contracts/http/openapi.yaml `data.purge_all` description (W4); contracts/ui/screens.yaml purge action copy (W3); acceptance/scenarios.yaml SC44 oracle (W6). No artefact may still say OWNER_DECISION_REQUIRED / PROV-PC01-03 for purge scope except as history. | W2, W3, W4, W6 |
| F-A2R5-02 (stale registered run) | Closing E0 run must be taken after every other packet of the wave has released (wait gate on W7's re-pin). | W6 |
| F-A2R5-03 (gate weaknesses) | `E0-12` eligibility = explicit **allowlist** of paths (contracts/modules.yaml, capabilities.yaml, ports.yaml, ops/deployment.md; contracts/data/*; schemas target/ingest-batch/report; contracts/state/*, errors.yaml, retry-policy.yaml; contracts/reporting/*; acceptance/fixtures/identity/**, acceptance/fixtures/reporting/**); `ratification_ref` must be a parsed header field (YAML front-matter / top-level key / `x-contract`), not prose; add mutation M6b to the self-test. | W6 |
| F-A2R5-04 (3 files outside scope at CONTRACT_READY) | contracts/ops/deployment.md **is** in scope 1 by explicit ruling (module placement and readiness are boundary contracts) — record the ruling in its header, not a source comment. Fixtures in the two ratified fixture directories (identity/ 14, reporting/ 14) are raised to CONTRACT_READY (they are the scope's acceptance oracles; their READMEs already claim it) with `ratification_ref` in each file's `x-contract`; every other fixture directory stays DRAFT. | W2 (deployment.md note), W3 (identity fixtures), W5 (reporting fixtures), W6 (allowlist) |
| F-A2R5-05 (schemas lack NOT_RUN statement) | target.schema.json, ingest-batch.schema.json (W3), report.schema.json (W5): add the runtime-NOT_RUN statement in `x-contract`. | W3, W5 |
| F-A2R5-06 (index.json honesty note) | Correct the note: seven audit reports in-repo under evidence/audits/, byte-identical to originals. | W6 |
| F-A2R5-07 (manifest dropped agent_profile files) | Next freeze includes the whole `agent_profile/` directory (registry.json as CANDIDATE, others DEPENDENCY). | Coordinator |
| Order | W2 ∥ W3 ∥ W4 ∥ W5 → W7 re-pin (`PC10-PIN-OD01c-20260907`) → W6 (SC44, E0-12 allowlist, index note, closing E0) → freeze e9 → A2-R6 scoped → commit. | |
