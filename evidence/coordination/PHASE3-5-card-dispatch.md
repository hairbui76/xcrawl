# Phase 3 (M3) + Phase 5 (M6, plain-text scope) card dispatch — 2026-09-08

Written to disk BEFORE dispatch (CR-PC00-30 lesson). Authority: `AUTH-OWNER-20260908-10` (Phase 5, record `packets/OWNER-DECISIONS-20260908-09.md`) and `AUTH-OWNER-20260908-06`/`-09` (Phase 3 entry: `REQ-OQ03` answered by `OD-20260908-06`, REQ-A5 Owner-signed `OD-20260908-08`; records `precode/owner-decisions-06.md`, `packets/OWNER-DECISIONS-20260908-08.md`). Owner instruction 2026-09-08: "spawn up to 10 Opus 5 high subagents, do until finish everything."

## Common rules — identical to `PHASE1-card-dispatch-template.md` items 1–8, with these updates
- Repo state: Phases 0–2 landed on `main` (`040bc5f` + this round's uncommitted precode/contract work). Read root `README.md`, `evidence/handoffs/P0-skeleton-handoff.md`, and the six Phase 1/2 card handoffs to see what exists (auth middleware, identity service, ingest, storage guard, research connector, collector client). Use generated models in `shared/rr_contracts`; use `tests/conftest.py` fixture loader.
- **WAIT GATE before any write**: poll (sleep 60, ≤30 min) until `agent-tasks/README.md` declares pin epoch `PC10-PIN-P3-20260908` AND `evidence/handoffs/PC10-handoff.md` carries a `PKT-PC10-FIX24` addendum with a released lease. Then verify your card's §0 pins against disk (SG-HASH) before the first write. Read everything else meanwhile.
- **Lease discipline**: write set = card §3 exactly + your Alembic revision(s) (`server/migrations/versions/<nnnn>_<card>.py`, must keep ONE head — merge revisions if you land after a sibling; assert `len(heads)==1` structurally, never a literal) + `evidence/handoffs/<card>-handoff.md` + `evidence/runs/<card>-E1-<UTC>.json`. Shared `server/app/main.py`: one delimited `# >>> <card> / # <<< <card>` include block only. Nothing under `precode/`, `contracts/`, `acceptance/`, `agent-tasks/` (precode freeze; contract defects → `CR-<card>-nn`).
- **Cross-card dependencies** (README §3 graph; each card §11 says which block start vs claim): if a dependency card's package is absent, code against `contracts/ports.yaml`'s operation signature via `server.app.<domain>.service` and mark the integration test `xfail(reason="pending TC-…")`; do not stub another domain inside your package; if a sibling in this same wave lands first, integrate with it for real. Scope your claim to what you actually proved.
- **Capabilities**: network only for `uv`/`npm` installs. **No live AI provider calls, no live Telegram calls, no browser, no secrets, no git mutation.** `PYTHONDONTWRITEBYTECODE=1`. Scratch `…/scratchpad/<worker>/`.
- **Quality bar** and **handoff** exactly as template items 7–8. Reply ONLY: status, files created, tests (pass/fail/xfail + command), manifest path, CRs (≤25 lines).

## Phase 3 — M3 (AI labelling and embedding)
Shared facts for all three: `REQ-OQ03` resolved (`contracts/ai/providers.yaml` §2.1: `anthropic@claude-sonnet-5` for `label`+`direction_phrasing`, `anthropic@claude-opus-5` for `summary`; both `api_key` family; both **`enabled: false`** — isolation ISO-03/ISO-05 unverified, E3 NOT_RUN). Your code must honour `enabled=false` (refuse to dispatch a real call; tests use recorded/fake transports). The `cli_acp` family is NOT chosen by the Owner: implement the interface the contract requires, but its adapter stays `enabled=false`, `isolation.* = unverified`, and `REQ-AC16` is reported **BLOCKED**, never FAIL (providers.yaml §4 `ac16_reporting_rule_vi`). Embedding = local `sentence-transformers` model per ADR-0011/REQ-D48/D50 (model download is a package install — allowed once; if the download itself is impossible in this environment, use a deterministic fake encoder behind the same port for tests and record NOT_RUN for the real-model dimension check).

### W3A — `agent-tasks/TC-analysis-adapter-validation.md`
Lease `LEASE-TC-ADAPTER-e1`, authority `AUTH-COORD-TC-ADAPTER`. Write set §3 (`worker/app/adapter/{base,api_provider,cli_acp,extract_json,validate}.py`, `tests/contract/test_analysis_result_schema.py`, `tests/integration/test_injection_canary.py`). Fixtures: the 9 `acceptance/fixtures/ai/*`. Oracles: `analysis-result.schema.json`; `AI_OUTPUT_INVALID` never commits `valid`; canary secret never leaves; tool-call count 0; network audit — no egress except the configured provider endpoint (and none at all in tests). `extract_json` deterministic, refuses >1 candidate, never "repairs" JSON. Usage `unknown` ⇒ all three fields null, never 0 (I14). Depends on `TC-analysis-once-per-generation` only for the task lifecycle (claim-scoped) — agree the `AdapterResult`/task-input types with W3B via the port; whoever lands first defines them in their own package, the other imports.

### W3B — `agent-tasks/TC-analysis-once-per-generation.md`
Lease `LEASE-TC-ANALYSIS-e1`, authority `AUTH-COORD-TC-ANALYSIS`. Write set §3 (`server/app/analysis/{service,key,router,repository}.py`, three tests). Depends on W3A (adapter) and ingest (done). Oracles: analysis key per ADR-0008 unchanged when only tags change ⇒ **provider call counter delta = 0**; resubmit same key ⇒ exactly one result; crash after provider completion ⇒ `unknown_attempt`, zero results; `secret.issue_task_credential` refused for a worker without the lease (ISO-05). Migration for the analysis tables the card names.

### W3C — `agent-tasks/TC-embedding-generation-switch.md`
Lease `LEASE-TC-EMBED-e1`, authority `AUTH-COORD-TC-EMBED`. Write set §3 (`server/app/embedding/{service,generation,repository}.py`, two tests). Depends on `TC-report-coverage-publish-cas` (Phase 4, absent) only for the "block before publish commit" proof — implement the guard at the embedding port and mark that one integration test `xfail(reason="pending TC-report-coverage-publish-cas")`. Oracles: cosine across two generations refused; activation atomic, no mixed generation; dimension/model id recorded per generation.

## Phase 5 — M6 Telegram, **plain-text scope only** (`OD-20260908-09`)
Shared facts: `contracts/telegram/delivery.md` 0.2.1 §3.4 — resolved: max message length 4096 chars; send limits 1 msg/s per chat, 20/min per group, ~30/s broadcast (planning ceilings; a real `Retry-After` always wins). **Unresolved (`BLOCKED_DEPENDENCY`): `callback_data` length, parse-mode escape table, buttons per row/keyboard.** Therefore, binding stop condition for all three cards: **no `parse_mode`, no inline keyboards, no `callback_data`-bearing buttons, no multipart branch that depends on an unresolved fact.** Plain-text `sendMessage` only; splitting by the 4096 limit using the "count the raw string" rule §3.4 locks; if a card's §8 test needs a callback/keyboard/parse-mode path, record it `NOT_RUN (BLOCKED_DEPENDENCY CR-PC07-04)` — do not implement it against a guessed number. Telegram transport = `httpx` (ADR-0011) behind a port with a recorded/fake transport in tests; **no live Bot API call**; webhook secret compared constant-time.

### W5A — `agent-tasks/TC-telegram-linking-auth.md`
Lease `LEASE-TC-TGAUTH-e1`, authority `AUTH-COORD-TC-TGAUTH`. Write set §3 (`server/app/telegram/{ingress,commands,linking,router}.py`, three tests). Commands allowlist exactly `CMD-status`, `CMD-run-now`, `CMD-save` as plain-text commands (no inline buttons). Unknown chat ⇒ **outbound call count 0, 0 mutations, 0 replies**. Link code single-use, rate-limited per chat. Depends on `TC-scheduler-lease-claim` (Phase 6, absent) for `run-now` — code against the port, xfail that path. Land first among the Phase 5 wave if you can: W5B and W5C integrate with your `linking`/`ingress` interfaces.

### W5B — `agent-tasks/TC-saved-snapshot.md`
Lease `LEASE-TC-SAVED-e1`, authority `AUTH-COORD-TC-SAVED`. Write set §3 (`server/app/saved/{service,snapshot,router}.py`, three tests). Canonical JSON + `content_hash`; snapshot survives source deletion, restart, restore (use `db/faults.py`); app + Telegram concurrent save ⇒ one snapshot. Save-from-chat path integrates with W5A's `CMD-save` (plain text); if W5A isn't there yet, xfail that one test. Note: the `saved_snapshot` table already exists (created in `0002b_shared_move_set_tables` on behalf of your module) — take ownership, don't recreate; adjust via a migration only if the contract requires columns it lacks.

### W5C — `agent-tasks/TC-telegram-unknown-delivery.md`
Lease `LEASE-TC-DELIVERY-e1`, authority `AUTH-COORD-TC-DELIVERY`. Write set §3 (`server/app/delivery/{service,parts,outbox,router}.py`, three tests). Oracles: timeout after the point a send may have happened ⇒ `unknown`, **0 automatic resends**; `unknown` leaves that state only via `delivery.decide_unknown` (owner); `TELEGRAM_PERMANENT_FAILURE` ⇒ report hash in DB unchanged; outbox intent committed before any send; sender lease. Multipart: parts are plain-text slices by the 4096 rule (allowed); "part 2 unknown doesn't resend part 1" is testable in plain text. Depends on `TC-report-coverage-publish-cas` (Phase 4, absent — report payload comes through a port; use a fake report row in tests) and `TC-backup-restore-drill` (Phase 6, absent — I15 post-restore reconciliation: implement the "dispatch locked until reconciled" flag check against the port, xfail the drill-driven test).
