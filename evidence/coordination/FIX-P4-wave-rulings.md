# Coordinator rulings — Phase 4/6 wave landed, pre-freeze fix round — 2026-09-08

All six cards landed (W4A report, W4B backfill, W4C ui-runs, W4D ui-reports, W6A scheduler, W6B backup). Repo: 990 passed / 10 xfailed / 0 failed / **18 setup errors** (one cause). Precode/contracts stay frozen. Disjoint write sets; W4B's follow-up is gated on W4A's.

| Item | Ruling | Owner |
| --- | --- | --- |
| **CR-TC-REPORT-09 (BLOCKING)** — W5C's `install_report_stand_in` inserts a 3-column `report` row; the real `report` table (0010) has `owner_id NOT NULL` etc. → 18 setup errors in `test_delivery_unknown_no_retry.py` (11) and `test_permanent_failure_report_intact.py` (7) | W5C replaces the stand-in with rows satisfying the real `report` schema (column list in W4A's handoff §7), or better, seeds through W4A's `report.publisher.publish_report` where the test needs a *published* report. No other change. | W5C |
| CR-TC-REPORT-08 — W3C's xfail imports `server.app.report.service` (path §3 never creates) | W3C re-points to `server.app.report.publisher.publish_report`; if W4A's guard call is real (W4A says the claim is proven from their side), make the test real; else keep `strict=True, run=True`. | W3C |
| CR-TC-REPORT-04 — `embedding/generation.py::cosine` uses `round()` (half-to-even); `contracts/reporting` §3 requires half-up | W3C switches to `decimal.ROUND_HALF_UP` at `SCORE_DECIMALS`; add the boundary case to the guard test. | W3C |
| CR-TC-BACKFILL-05 — `builder._candidates` never unions the backfill extension; no deployment consumes an entitlement | W4A adds a `backfill_port` parameter to `build_report` (default = no-op port) that unions entitled items into candidates; W4B's positive-path test then runs for real. | W4A → W4B |
| CR-TC-BACKFILL-10 — `backfill_ledger.tag_id` FK withdrawn because W4A's test seeds no `tag` row | Leave withdrawn (0002b precedent, `tag` table is W4B's custodian creation); CR to PC02 for the FK in a later rebuild. | — |
| CR-TC-BACKFILL-11 — `scenarios.yaml` SC22 oracle names `pending_item_ledger.report_id`/`consumed_at` (absent) and expects 2 items vs fixture `e`'s 1 | Oracle defect → CR to PC09/PC04 (scenarios.yaml is frozen); W4B's `SG-PC09` stop is correct; claim stays scoped. | W6n (PC09-P4 records) |
| CR-TC-uiruns-01 — `screens.yaml §run_status_labels` overlapping/incomplete | Precedence `run_failed > stopped_early > no_matching_content` as W4C derived from fixture `m` + I13 stands; CR to PC07. | — |
| **OD-20260908-10 item 1** — save without analysis → allowed with a "not analysed yet" label | W5B implements: `save.create` succeeds, summary block marked missing with a fixed literal (no inference), `snapshot.content_hash` over the actual stored bytes; tests for the fixture(s) that cover it; CR for the schema field if `saved-snapshot.schema.json` lacks a slot for the marker (then use the existing optional summary = null + an explicit `summary_state: missing` if the schema allows; else STOP with the CR — do not add an undeclared column, `test_schema_matches_entities.py` will catch it). | W5B |
| OD-20260908-10 items 2–3 | No code change (`/save <id>` kept; reminder kept). Fixture CR to PC08 for item 3. | W1n records; W6n PC09-P4 |
| Older xfail premises | Every card re-checks its own `pending TC-…` xfails against the tree now that report/scheduler/backup exist; make real or keep `strict=True, run=True`. | W3B (pending_item_ledger → W4B's `PendingLedgerPort`), W5A (`run-now` → W6A), W5C (drill → W6B), W4C (shape agreement with W6A/W5C → integration test if feasible) |
| Order | W5C ∥ W3C ∥ W4A ∥ W5B ∥ W3B ∥ W5A ∥ W4C → W4B (after W4A) → Coordinator re-verify → W1n records OD-10 (precode, single writer) → WP re-pin → freeze FC-P4 → A3-P4-R1 → PC09-P4 → packaging → commit → final report. | |
