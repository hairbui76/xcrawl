# AUDIT_REPORT — PKT-A1-R1 · frozen candidate FC-W1 (epoch 1)

## 1. Frozen reference

| Field | Value |
| --- | --- |
| candidate | `FC-W1` · epoch **1** |
| manifest | `…/scratchpad/audits/FC-W1-manifest.txt` · 54 entries |
| manifest algorithm | `sha256-path-role-hash-bytes-v1` (protocol §6) |
| manifest_sha256 **recomputed at audit start** (2026-09-07, first action) | `93edaab92fe57467e761217bee482e6f8ac3800fd1f8dd894e8bcadc3c41d084` |
| manifest_sha256 **recomputed at audit end** (last action) | `93edaab92fe57467e761217bee482e6f8ac3800fd1f8dd894e8bcadc3c41d084` |
| declared in manifest header | `93edaab92fe57467e761217bee482e6f8ac3800fd1f8dd894e8bcadc3c41d084` — **matches** |
| per-file rehash | 54/54 sha256 **and** byte counts match; 0 mismatches, 0 missing, both at start and at end |
| entry ordering | verified sorted by UTF-8 bytes of canonical path; no `\|`/CR/LF/TAB/control chars in paths |
| sources | SRC-PLAN `f65bb04657f30f1dffe9c96f2667ed9ca1bb1d9363cca3d857abb15034707f40` (64915 B) · SRC-SPEC `d35e1f2daab30e7ab36969ecd9b9b0f227d8ab70fbf55482af47c4a1405e0e26` (41770 B) — both match baseline §2; the two immutable copies under `precode/source/` are byte-identical to them (hash equality proven by the manifest rehash) |
| snapshot immutability | manifest recomputed identical at start and end of the review interval; no `contracts/state/` or `contracts/reporting/` file appeared during the audit (tree enumeration below returned zero unmanifested extras) |
| completion ceiling of candidate | `DRAFT_FOR_REVIEW` — no file in the candidate asserts more (verified by scan) |

**No STALE condition.** Verdicts below are pinned to exactly these bytes.

## 2. Reviewer identity and independence declaration

Reviewer principal: `auditor-A1`, dispatched by the Coordinator under `AUTH-COORD-A1-R1` (parent `AUTH-OWNER-20260906-01`), packet `PKT-A1-R1`, lease_id `null`.

**Independence declaration.** I have authored nothing in `/mnt/virtual/repo/xcrawl`. I did not write, co-author, edit, patch, rename or delete any file in the repository at any point, under this or any prior principal identity. I hold no write lease and created none. The candidate was authored by `worker-W1` (PC00), `worker-W2` (PC01, PC01-FIX1) and `worker-W3` (PC02, PC02-FIX1); I am none of them and share no lineage with them. No authorship conflict exists; `BLOCKED_INDEPENDENCE` does not apply.

Read-only discipline observed: every helper script lived under `…/scratchpad/a1/` and ran with `PYTHONDONTWRITEBYTECODE=1`; the only repository command executed was `git status --porcelain` (read-only). Post-audit verification: `git status --porcelain` shows only the five pre-existing untracked directories (`.claude/ acceptance/ contracts/ evidence/ precode/`); `find /mnt/virtual/repo/xcrawl -name '__pycache__' -o -name '*.pyc'` (excluding `.git`) returns empty. No network access. No subagents spawned. This report is written only to the Coordinator-designated evidence sink outside the project tree.

## 3. Scope checked / excluded

### Checked
- Manifest integrity and completeness against the three handoff `changes` tables, incl. both FIX1 addenda (post-fix bytes).
- Unmanifested-extras sweep over `precode/`, `contracts/`, `acceptance/`, `evidence/`.
- **PC00**: `precode/baseline.json`, `requirements.csv`, `decision-register.md`, `owner-decision-request.md`, `adr/README.md`, `ADR-0001…0010`, `source/*`.
- **PC01**: `contracts/modules.yaml`, `capabilities.yaml`, `ports.yaml`, `ops/deployment.md`.
- **PC02**: `contracts/data/entities.yaml`, `identity.md`, `invariants.md`, `contracts/schemas/{target,ingest-batch}.schema.json`, all 14 fixtures + fixture README.
- Both sources read in full; `agent_profile/auditor.md` and `protocol.md` read in full.
- Cross-package consistency, ID conventions (baseline §3), header fields, language rule, claim ceilings, forbidden labels.

### Excluded (and why)
- `contracts/state/*`, `contracts/reporting/*` and every other PC03–PC10 deliverable — out of packet scope; none existed in the tree during the audit.
- Runtime behaviour of anything. Every enforcement claim in the candidate is `NOT_RUN`; I verify **contract text and static consistency (E0)** only. A fixture that parses does not prove a guard exists; a manifest hash does not prove provenance.
- Product correctness of the provisional decisions themselves. Those are the Owner's to ratify; I check only that they match baseline §5 and are labelled `PROVISIONAL`.
- Prose quality/accuracy of Vietnamese text beyond the 25-row fidelity sample.

## 4. Evidence records (all `INDEPENDENT_AUDIT`, producer `auditor-A1`)

Runtime: Linux 7.0.0-30-generic, python3 3.12.3, PyYAML 6.0.1, jsonschema 4.10.3. All scripts under `…/scratchpad/a1/`, `PYTHONDONTWRITEBYTECODE=1`. Started 2026-09-07; each command's stdout was read in full, not merely its exit status.

| ID | Command / procedure | Oracle | Expected | Observed | Exit | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `EV-A1R1-01` | `python3 a1/verify.py` (rehash all 54 entries, rebuild manifest hash per protocol §6) | per-entry sha256 + bytes match; recomputed manifest hash == declared | 0 mismatches | 54/54 match; recomputed `93edaab9…d084` == declared; sort order valid | 0 | **PASS** |
| `EV-A1R1-02` | `find precode contracts acceptance evidence -type f` diffed against manifest paths | no file in those trees outside the manifest | 0 extras | 0 extras, 0 missing | 0 | **PASS** |
| `EV-A1R1-03` | `python3 a1/c1.py` + `c2.py` (requirements.csv parse; ID-form regex; coverage vs regex-extracted source lists; **status fidelity** D-row status vs spec TT column) | 246 rows, unique ids, all statuses ∈ {XN,UQ,ĐX,KC}, every D/AC/P0/P1/OOS/A/OQ present, every D status equals the spec's | 0 violations | 246 rows, 0 dup, 0 ID-form violations; 59/59 D-rows present incl. `REQ-CTAG`; **0 status mismatches**; AC-01..18, P0-01..12, P1-01..05, OOS-01..06, A1..A7, OQ01..10 all present | 0 | **PASS** |
| `EV-A1R1-04` | `python3 a1/c5.py` (every `source_anchor` resolves to an `anchor_id` in `baseline.json`; **line numbers spot-checked against the real source files** for all 59 decisions, 57+44 sections, 17 blockers, 15 invariants, 16 error codes, 9 gates) | 0 unresolved anchors, 0 line-number mismatches | 0 | 294 anchor_ids; 246/246 anchors resolve; 0 line mismatches in any group | 0 | **PASS** |
| `EV-A1R1-05` | Manual read of 25 sampled rows across all categories (`REQ-D01/D08/CTAG/D25/D28/D29/D35/D53/D54/D57/D58`, `AC03/04/07/14/16/18`, `P0-03/P0-12`, `A5/A7`, `OQ01/OQ10`, `P1-01`, `OOS-02`) against the pinned source text | text is a quote or close paraphrase of the cited anchor; notes do not invent obligations | ≥24/25 faithful | **25/25 faithful**; anchors correct; `notes` correctly carry the amendment/PROVISIONAL context | n/a (manual) | **PASS** |
| `EV-A1R1-06` | `python3 a1/c6.py` (forbidden-label scan over every CANDIDATE + EVIDENCE file, sources excluded) | no `TBD` as a value, no `status: CLOSED/ACCEPTED`, no claim > `DRAFT_FOR_REVIEW`, no "independent audit passed" | 0 asserted | 6 textual hits, **all read manually**: every `CONTRACT_READY` occurrence is a negation ("không đạt/không tuyên bố `CONTRACT_READY`"), both `TBD` occurrences are quotations of the prohibition itself, `baseline.json` lists the ceiling vocabulary as data. 0 asserted violations | 0 | **PASS** |
| `EV-A1R1-07` | `python3 a1/c-amd.py` (decision-register structure) | exactly B01..B17 once each; each `PROVISIONAL`; every referenced `AMD-B..` defined; each amendment has Trước/Sau/Vì sao/Bảo đảm thay thế/Test-oracle/REQ | 0 violations | 17/17 blockers, each once, all `PROVISIONAL`, `decision_owner: Owner`; 14 amendments, **all 6 fields present in all 14**; 0 referenced-but-undefined AMD | 0 | **PASS** |
| `EV-A1R1-08` | Manual read of all 17 blocker entries against baseline §5 rulings | each provisional resolution equals the baseline §5 row; each has quoted both sides, impacted contracts, gate, oracle change, "if Owner rejects" | 17/17 | **17/17 faithful to baseline §5**, no drift, no self-granted product decision | n/a | **PASS** |
| `EV-A1R1-09` | `python3 a1/z1.py` (ADR shape + index) | 10 files, 6 required sections each, each listed in README | 10/10 | 10/10 complete and indexed | 0 | **PASS** |
| `EV-A1R1-10` | `python3 a1/p1.py` + `p2.py` + `p3.py` (**independent re-run of PC01 EV-02/EV-04**: uniqueness, ID form, owner/caller existence, inbound==ports, bidirectional inbound/outbound, allowed_edges == derived caller×owner product, forbidden reasons, auth-scope closure, one-owner-per-mutation) | 0 violations | 0 | **82 operations, 0 duplicates, 0 ID-form violations; 24 modules + 7 external systems; inbound union == ports set exactly; 96 allowed_edges == 96 derived edges (set-equal, both directions); 36/36 forbidden edges carry a non-empty reason; 10/10 auth scopes defined; 56 mutating operations, each in exactly one ownership scope, owner == `owner_module`; 0 mutations lacking idempotency**. PC01's EV-PC01-04 numbers reproduce exactly. | 0 | **PASS** |
| `EV-A1R1-11` | Manual diff of `ports.yaml` operation list against the PC01 packet's minimum operation list (15 groups) | every named operation present | all present | All present: auth login/logout/session · settings read/update · tag CRUD+preview+rescan · run list/detail/run_now/resume/cancel · worker claim/heartbeat/ingest_batch/checkpoint/report_stop/release · research fetch (internal) · analysis claim/heartbeat/submit_result/report_attempt_unknown · embedding generate + generation switch · report build/publish/list/detail · save create/unsave/list/export (flagged `deferred_p1`) · telegram ingress/link issue+consume/unlink/send/outbox dispatch/receipt · delivery status + decide_unknown · backup create/verify/restore/reconcile · health+readiness | n/a | **PASS** |
| `EV-A1R1-12` | Error-code closure check over all 82 operations | all 16 plan §10 codes used; the 5 packet-required generic codes present; any new code declared PROVISIONAL with a CR | 0 unregistered | 23 codes used: **16/16 plan §10 codes present**, plus `UNAUTHORIZED`, `IDEMPOTENCY_CONFLICT`, `STALE_LEASE`, `VALIDATION_ERROR`, `NOT_FOUND`, plus `CAPABILITY_DENIED` and `CONFLICT` — both declared PROVISIONAL with `CR-PC01-01`/`CR-PC01-03` to PC03 | 0 | **PASS** |
| `EV-A1R1-13` | `python3 a1/q1.py`+`q2.py`+`q3.py` (**independent re-run of PC02 EV-01/02/03**: `Draft202012Validator.check_schema`; every `kind: work\|post` object validated against `target.schema.json` with format checker; every fixture batch validated against `ingest-batch.schema.json`; `payload_hash` recomputed as `sha256(JCS(payload_core))`) | schemas valid; all target objects valid; declared accept/reject outcomes reproduce; positive payload hashes reproduce | 0 violations | Both schemas pass the 2020-12 metaschema with correct `$id`s. **39 target objects validated, 0 invalid.** **9 batch cases: 4 `accept` accepted, 5 `reject` rejected**, each for the stated reason (`required property`, `pattern sha256`, `too short`, `additionalProperties`, `pattern timestamp`). **4/4 positive `payload_hash` values recompute exactly**; the 2 mismatches are on negative fixtures whose payloads are deliberately mutated. | 0 | **PASS** |
| `EV-A1R1-14` | Entity-coverage check vs spec §7.1 + the PC02 packet's required additions; `owner_id` presence sweep | all 18 spec §7.1 entities + all 15 packet additions present; `owner_id` on every entity except declared exceptions | 0 missing | 37 entities: **18/18 spec §7.1 present, 15/15 packet additions present**; entities without `owner_id` = exactly `['owner','schema_migration']`, matching the declared exceptions | 0 | **PASS** |
| `EV-A1R1-15` | Manual read of `unique_constraint_semantics`, `target_union`, `saved_item.keys`, `limits`, `invariants.md` I02/I03/I04/I08/I15/I16/I17, `identity.md` §2/§5/§6/§9 | each UNIQUE states guarantees **and** non-guarantees; B06 "two UNIQUEs are not enough" stated explicitly; Saved UNIQUE on the union key; every limit has value+unit+status+decision_ref+rationale; each invariant has statement/owner/positive/counterexample/oracle/evidence | conforms | Conforms, and to a high standard: `b06_explicit_statement` gives a concrete counterexample; `UNIQUE(owner_id, target_key) WHERE state='active'` is on the union key with the NULL-inequality rationale spelled out; all 8 limits carry value+unit+PROVISIONAL+decision_ref+rationale; I02/I03/I04/I08 each carry ≥3 counterexamples plus a row-count/hash oracle table; `identity.md` §9 records fuzzy-title merge and AI-decided merge as `KC`/forbidden | n/a | **PASS** |
| `EV-A1R1-16` | `python3 a1/hdr.py` (baseline §3 header fields across every contract/schema/fixture/registry file) | 14 mandatory fields present | all present | 4 YAML contracts, 4 prose contracts, 2 JSON schemas (`$id`+`title`+`description`+`x-contract`), fixture README, decision-register, owner-decision-request: **all 14 fields present, `status: draft`, `claim_ceiling: DRAFT_FOR_REVIEW`**. Two exceptions found → `F-A1R1-05`, `F-A1R1-06`. | 0 | **PASS with findings** |
| `EV-A1R1-17` | `python3 a1/x1.py` (**check PC02 explicitly left unrun, CR-PC02-11**: every fixture event `(actor, operation)` against `ports.yaml.caller_modules` and `modules.yaml.allowed_edges`) | every event names an allowed caller | 0 violations | **22 events; 12 consistent; 10 violate default-deny** | 1 | **FAIL** → `F-A1R1-02` |
| `EV-A1R1-18` | `python3 a1/x2.py` (PC02 entity names ↔ PC01 `data_owner_of`) | every entity has exactly one PC01 owner | 0 unowned | **9 of 37 PC02 entities absent from PC01 `data_owner_of`**; 29 PC01 owner tokens have no PC02 entity; 0 owner-module disagreements among the matched names | 1 | **FAIL** → `F-A1R1-03` |
| `EV-A1R1-19` | `python3 a1/z1.py` (PC02→PC01 reference existence, **re-run of PC02 EV-PC02-04**) | every `MOD-…` in PC02 files exists in modules.yaml; renamed/removed tokens absent | 0 dangling | 0 dangling `MOD-` ids; every stale token (`MOD-report-builder`, `MOD-backup-operator`, `saved.create`, `analysis.request_generation`, `source.fetch_metadata`) gone; the 3 residual `post.mark_source_deleted` strings are all **explicit negations**, matching the FIX1 claim | 0 | **PASS** |
| `EV-A1R1-20` | `python3 a1/y1.py` (YAML data-shape sweep: list items that silently parsed as single-key mappings) | homogeneous lists | 0 | 3 accidental mappings in one list | 1 | **FAIL** → `F-A1R1-07` |
| `EV-A1R1-21` | Fixture (a)–(h) arithmetic walkthrough: `given` rows + `events` ⟹ `expected.counts` | expected counts derivable from given+events | 8/8 derivable | **8/8 internally consistent.** e.g. (a) given 2 works + 2 aliases, +1 OpenAlex alias ⟹ `identity_alias__total: 3`, `work__total: 2` (1 active, 1 merged), `identity_merge_audit: 1` ✓; (h) 6 items ⟹ `post: 6`, `post_work: 6`, `work__total: 1`, `identity_alias__total: 1`, `report_item: 1` ✓; (d) event 2 + event 3 `repeat: 4` ⟹ `telegram_bot_responses: 5` with `saved_item__total: 1`, satisfying AC-13 ✓ | n/a | **PASS** |
| `EV-A1R1-22` | `denied_cases` + `default_deny` inspection | 10 named negative cases, each with error code + enforcement; default-deny stated in-file | 10/10 | NC-01..NC-10 present, matching the packet's ten cases 1:1, each with `expected_error_code`, `expected_state_vi`, `enforcement`, `enforcement_detail_vi`; `default_deny.rule_vi` states the rule **and** warns that `forbidden_edges` is not exhaustive. Enforcement status is honestly `NOT_RUN` for all ten. | 0 | **PASS** |
| `EV-A1R1-23` | SC-ID convention check across ports.yaml and fixtures | SC refs follow baseline §3 (SC01–18 = AC-01–18; SC19–28 = plan §13 list in order) | consistent | SC01–SC28 all used; SC21 = "ingest ACK mất" on `ingest.submit_batch` ✓, SC23 = "identity conflict" on ingest/merge ✓ — ordering honoured. PC02 introduces SC29/30/31 with `CR-PC02-08` to PC09 ✓. Anchor-side gap → `F-A1R1-08`. | n/a | **PASS with finding** |
| `EV-A1R1-24` | Requirement-registry enum + prose-coverage check | `blocked_by` ∈ B01..B17; `impacted_packages` ⊆ PC00..PC10 and non-empty; no embedded newlines; every packet-required prose section represented | 0 violations | 0 enum violations; 0 embedded newlines; **all 28 required prose sections present** (129 `REQ-S…` rows) | 0 | **PASS** |
| `EV-A1R1-25` | Fixture evidence-honesty sweep | no fixture claims a run result | all `NOT_RUN` | 14/14 fixtures: `evidence_status: NOT_RUN`, `claim_ceiling: DRAFT_FOR_REVIEW` | 0 | **PASS** |

**Evidence-honesty conclusion.** Every `PASS` asserted in the three handoffs that I could re-run, I re-ran; all of them reproduce (EV-A1R1-03/04/07/09/10/13/14/19). The handoffs' `NOT_RUN` declarations for E1–E4 and for independent audit are accurate, and none of the three writers claimed independent review. The two FIX1 addenda's before/after hashes are consistent with the frozen bytes. PC02's `A5` drift warning about reading PC01 mid-flight is disclosed rather than hidden. I found no fabricated verification.

## 5. Findings

All findings are **OPEN**. I do not close, waive, or accept them, and I supply no patch or candidate bytes.

| ID | Sev | File / ref | Condition (reproduce) | Expected vs observed | Impact | Remediation constraint (what must become true) |
| --- | --- | --- | --- | --- | --- | --- |
| **F-A1R1-01** | **MAJOR** | `contracts/ports.yaml` → `ingest.commit_checkpoint`; `contracts/data/invariants.md` → I02 counterexample 2; `contracts/data/entities.yaml` → `transactions[TXN-ingest-batch]`; `contracts/modules.yaml` → `data_owner_of` | Read PC02's I02: counterexample **2** is *"Checkpoint ở transaction riêng. Ghi post ở T1, ghi checkpoint ở T2 … → Gate FAIL."* Then read PC01's `ingest.commit_checkpoint`: a standalone `http` mutation owned by `MOD-ingest-service` with its own idempotency key `assignment_id + checkpoint_seq`, i.e. a checkpoint write in a transaction separate from the batch. | **Expected:** one frozen account of how a checkpoint becomes durable. **Observed:** three. (i) PC02's `TXN-ingest-batch` writes `checkpoint` inside the ingest transaction; (ii) PC01 defines a second, independent operation that writes a checkpoint; (iii) PC01's `data_owner_of` declares **two** owner tokens — `ingest_checkpoint` (`MOD-ingest-service`) *and* `run_checkpoint_pointer` (`MOD-job-service`) — where PC02 models a single `checkpoint`. No transaction map, commit point, or external-observer timeline exists anywhere for `ingest.commit_checkpoint`; PC02's transaction list does not mention it (grep: 0 hits in all five PC02 contract files). | I02 is the invariant gating G2. An implementer reading the frozen inventory may build exactly the write path the invariant contract declares a must-fail, and no oracle covers it. Cross-module ownership of the ACK cursor is ambiguous between two modules. | Either (a) `ingest.commit_checkpoint` is withdrawn and the checkpoint proposal travels only inside `ingest.submit_batch` (as `client_checkpoint_proposal` already allows), or (b) it survives with an explicit transaction contract — rows written together, commit point, failure timeline, and an oracle proving it cannot advance `acked_through_ingest_sequence` past durable posts — **and** I02's counterexample 2 is re-worded so it does not condemn the sanctioned path. In either case exactly one module must own the checkpoint table, and `ingest_checkpoint` / `run_checkpoint_pointer` / `checkpoint` must resolve to one named entity. A ruling alone is not sufficient: the surviving path needs a stated oracle. |
| **F-A1R1-02** | **MAJOR** | `acceptance/fixtures/identity/{a,b,c,e,g,h}-*.json` → `events[].actor` vs `contracts/ports.yaml.caller_modules` and `contracts/modules.yaml.allowed_edges` | `python3 a1/x1.py` — for every fixture event, check `(actor, operation)` against the frozen registry. | **Expected:** under default deny, a fixture event must name an allowed caller. **Observed:** 22 events, **12 consistent, 10 violating** — `a`#1, `c`#1, `g`#3 (`MOD-research-connector` "calls" `research.fetch_work_metadata`, which it *owns*; the caller is `MOD-ingest-service`); `a`#2, `c`#2 (`MOD-backend-api` calls `identity.merge_works`; allowed callers are ingest/identity only); `b`#2, `g`#2, `h`#2 (`MOD-backend-api` calls `identity.resolve_target`; allowed are ingest/report); `g`#4 (`MOD-backend-api` calls `analysis.request_reanalysis`; allowed is `MOD-web-ui`); `e`#2 (`MOD-ingest-service` "calls" `ingest.submit_batch`, which it owns — the caller must be `MOD-x-collector`). | These fixtures are the acceptance oracles for AC-07/AC-09/AC-12/AC-13 and for I02/I03/I08. Each violating event asserts a communication edge the module registry forbids, so a conforming implementation cannot reproduce the fixture, and a test harness built from them would encode a forbidden edge. PC02 anticipated the *absence* of this check (`CR-PC02-11`/M6) but never ran it; the defects were therefore never reported. | Every `events[].actor` must be a declared caller of that operation in `ports.yaml` **and** the `(caller, owner, operation)` triple must exist in `allowed_edges` — or the fixture must use a field that is explicitly not a caller assertion (e.g. `performed_by`) with that meaning defined in the fixture README. The check must become an automated gate (the pass PC02 requested from PC09), not a one-off correction, and it must be re-run against the corrected fixtures before the next freeze. |
| **F-A1R1-03** | **MAJOR** | `contracts/modules.yaml` → `modules[].data_owner_of`; `contracts/data/entities.yaml` → `entities[].owner_module`; PC02 handoff §A6 M1–M5 / `CR-PC02-10`, `CR-PC02-09` | `python3 a1/x2.py` — set-compare the 37 PC02 entity names against the 57 PC01 `data_owner_of` tokens. | **Expected:** every entity resolves to exactly one owning module across the two contracts. **Observed:** **9 of 37 PC02 entities have no PC01 owner**: `identity_alias`, `checkpoint`, `assignment_lease`, `coverage_window`, `tag_vector`, `source_connection`, `outbox_intent`, `schema_migration`, **`identity_merge_audit`**. PC02's `CR-PC02-10` accounts for 5 as renames and `CR-PC02-09` for 3 as unowned — **`identity_merge_audit` is in neither list and was never reported**, although PC01-FIX1 renamed a mutation scope to "… / merge audit" without adding the table to `MOD-identity-service.data_owner_of`. In the other direction 29 PC01 owner tokens have no entity contract, including `first_announced_ledger`, which PC02's I07 interface and `CR-PC02-06` both depend on. | 24% of the entity model has no unambiguous data owner, so the G1 claim "mỗi mutation có đúng một owner" holds at operation level but **not** at table level. `identity_merge_audit` is the audit trail that makes B15/I03 ("no guessed merges") auditable; an unowned audit table has no module accountable for writing it. | A single naming authority must issue one ruling covering **all nine** names, not the five/three the writers enumerated, and `identity_merge_audit` must be assigned an owner in `data_owner_of`. The 29 PC01-owned tokens without an entity contract must each be either mapped to an existing entity or explicitly assigned to a later package with a named target file, so that no owner token is orphaned. Both directions must be re-verified by a set-comparison, not by inspection. |
| **F-A1R1-04** | **MAJOR** | `contracts/ports.yaml` → `save.remove`; `contracts/modules.yaml` → `unresolved_refs[PROV-PC01-03]`; `precode/requirements.csv` → `REQ-S7.3-05` | `REQ-S7.3-05` (status **XN**) records SRC-SPEC §7.3: *"Bỏ lưu ≠ xóa dữ liệu gốc ≠ xóa toàn bộ dữ liệu. Ba thao tác riêng, thao tác thứ ba đòi xác nhận gõ tay."* Grep the 82-operation inventory for the second and third. | **Expected:** three distinct operations, or an explicit block on each missing one. **Observed:** only `save.remove` exists. `PROV-PC01-03` flags **only** the third ("xóa toàn bộ dữ liệu"). The **second** ("xóa dữ liệu gốc") is absent from the inventory and absent from every unresolved-ref list — silently uncovered. Separately, `save.remove.requirement_refs` cites `REQ-S7.3-01` (the `owner_id` rule) instead of `REQ-S7.3-05`, so the row that carries this obligation has no operation pointing back at it. | `ports.yaml` is declared the single operation inventory that PC03–PC08 "cite verbatim". A confirmed (XN) requirement with a destructive-action distinction is one-third covered and two-thirds unaccounted; a downstream package citing the inventory will not discover the gap. It also breaks the plan §17 DoR item "không orphan requirement". | Both missing operations must be either (a) added to the inventory with auth scope, confirmation semantics, transaction and error map, or (b) recorded as explicitly blocked scope with `OWNER_DECISION_REQUIRED` — **each named separately**, since they differ in blast radius. `save.remove.requirement_refs` must cite `REQ-S7.3-05`. Coverage must be demonstrated by a requirement→operation traceability check, not by prose. |
| **F-A1R1-05** | MINOR | `precode/adr/ADR-0001…ADR-0010` front-matter | `python3 a1/hdr.py` — check baseline §3's 14 mandatory header fields on every contract/registry markdown file. | **Expected:** baseline §3 states *every* contract/schema/fixture/registry file carries the plan §5 mandatory fields, markdown via front-matter. **Observed:** all 10 ADRs omit 8 of them (`contract_id`, `version`, `owner_role`, `decision_refs`, `producers`, `dependencies`, `scope`, `verification`), carrying an ADR-specific set instead. The PC00 packet's own checklist item 5 prescribes that ADR shape, so the rule and the packet conflict — but PC00's handoff §6.3 declares **only** the `requirements.csv` deviation, not this one. | An undeclared deviation from a binding convention; PC09's static header check will flag 10 files with no recorded justification. | Either baseline §3 is amended to exempt ADRs (with the exemption written down and referenced from the ADR index), or the ADRs gain the missing fields. Whichever is chosen, the deviation must appear in a handoff/decision record as a declared exception, not be left implicit. |
| **F-A1R1-06** | MINOR | `precode/baseline.json` → `requirements_csv_contract_header`; PC00 handoff §6.3 | Read the substitute header PC00 created to stand in for `requirements.csv`'s missing front-matter, and compare to baseline §3's minimum field list. | **Expected:** the substitute carries the mandatory fields it substitutes for. **Observed:** `requirement_refs` is missing from the object; the handoff §6.3 enumerates the fields it believes it supplied and that list also omits `requirement_refs`, so the omission is systematic rather than a typo. The handoff's claim that the substitute holds "các trường bắt buộc" is therefore slightly overstated. | The one file allowed to deviate from the header rule does not fully satisfy the rule it was allowed to deviate *into*; a header-completeness check would pass the deviation while the deviation itself is incomplete. | `requirements_csv_contract_header` must contain every baseline §3 minimum field, and the handoff statement describing it must match its actual contents. |
| **F-A1R1-07** | MINOR | `contracts/capabilities.yaml` → `worker_capability_registration.payload_fields[8].item_fields` | `python3 a1/y1.py` — sweep for list items that parse as single-key mappings. | **Expected:** `item_fields` is a homogeneous list of descriptive strings. **Observed:** 3 of 8 entries parse as mappings because unquoted `"… (enum: a \| b)"` text is read as YAML `key: value` — `{'auth_family (enum': 'api_key \| cli_acp)'}`, `{'usable (enum': 'usable \| unusable \| unknown)'}`, `{'tasks_supported (array': 'open_labeling \| summary \| emerging_direction_phrasing)'}`. The file parses without error, so PC01's parse-only oracle (EV-PC01-01) could not detect it. | This list is the declared "nguồn sự thật duy nhất" for whether a CLI/ACP provider is usable (B13, AC-16). A consumer iterating `item_fields` receives mixed types and the enum values are split across a key/value boundary. | The three entries must be quoted so they parse as strings — or, better, `item_fields` must become a structured list of `{name, type, values, required}` objects. The validating oracle must be strengthened beyond "parses without exception" to a shape assertion, otherwise the same class of defect stays invisible. |
| **F-A1R1-08** | MINOR | `precode/baseline.json` → `source_anchors.SRC-PLAN.scenarios` | Read the scenario anchor list. | **Expected:** baseline §3 fixes `SC19` … `SC28` as individually stable IDs (plan §13 list, in order), and PC00's checklist item 1 requires an anchor for every SC of the plan. **Observed:** 19 anchors — SC01–SC18 individually, then a single collapsed `SRC-PLAN:SC19..SC28`. Meanwhile `ports.yaml` cites SC19–SC28 **individually** (all ten used), so those ten citations have no per-ID anchor to be checked against. I verified the two I could infer (SC21 = "ingest ACK mất", SC23 = "identity conflict") are correctly ordered, but that ordering is currently unenforceable. | PC09's traceability pass cannot mechanically validate ten scenario citations already in use by a frozen contract; a mis-numbering would go undetected. | Each of SC19–SC28 needs its own anchor entry with the plan §13 line and its one-line subject, so that scenario citations in `ports.yaml` and the fixtures can be checked by ID rather than by reading. |
| **F-A1R1-09** | MINOR | `precode/requirements.csv` → `REQ-S4-05`; `contracts/ports.yaml` → `save.export`; PC00 handoff §6.1 `F-PC00-02` | Read `REQ-S4-05` (Saved screen, status **UQ**), which lists `export` among the screen's MVP actions, then read `save.export` (`lifecycle_status: deferred_p1`). | **Expected:** a self-made product decision (defer Saved export to P1) is traceable from the requirement row it overrides. **Observed:** `REQ-S4-05` lists `export` as an MVP action with no reference to `F-PC00-02`, to `REQ-OQ10`, or to the deferral; its `notes` mention only that the IA table is a delegated presentation detail. The registry and the operation contract therefore disagree about whether export is in MVP, with no link between them. | `F-PC00-02` is one of the two decisions PC00 had to make outside baseline §5, and `CR-PC00-02` already warns it will "rơi khỏi tầm nhìn". A reader of the registry alone would build export into MVP. | `REQ-S4-05` must carry the deferral note and cross-reference `REQ-OQ10` / `F-PC00-02`, so that the registry, the ports contract and the Owner request agree. The same cross-reference discipline should apply to `F-PC00-01` (unlink) on `REQ-D37` — where it is, correctly, already present. |

**Severity counts: CRITICAL 0 · MAJOR 4 · MINOR 5 · total 9.**

## 6. Independent view on the items the Coordinator flagged

Requested explicitly, and not treated as pre-judged. These are my own conclusions from the frozen bytes.

- **`CR-PC02-10` (entity-name divergence).** Real, and **under-reported**. The five named pairs (`identity_alias`/`work_alias`, `checkpoint`/`ingest_checkpoint`, `assignment_lease`/`lease`, `coverage_window`/`coverage_ledger`, `tag_vector`/`vector`) are genuine and each is a pure rename with no semantic conflict — a naming ruling resolves them cleanly. But the measured divergence is nine entities, not five plus three: see `F-A1R1-03`. My view is that the ruling should be issued as one authority over the whole set, and that PC02's offer to absorb it in a FIX2 is the right shape, provided the ruling also assigns `identity_merge_audit` and resolves the `checkpoint` triple-definition from `F-A1R1-01` at the same time — those two are coupled and fixing them separately will reintroduce drift.
- **`CR-PC02-09` (three entities with no PC01 owner, assigned PROVISIONAL).** PC02's provisional assignments are each defensible on the evidence: `source_connection` → `MOD-settings-service` mirrors `provider_config`'s owner; `outbox_intent` → `MOD-delivery-service` follows from that module owning `delivery.create_intent`/`delivery.dispatch_next`; `schema_migration` → `MOD-data-store` follows from that module owning `sqlite_database_file`/`wal`. Marking them PROVISIONAL rather than guessing silently was the correct call under baseline §6. My reservation is only that the list was incomplete (`identity_merge_audit`), so ratifying `CR-PC02-09` as written would leave a hole.
- **`PROV-PC01-03` (no delete-all-data operation).** PC01's restraint — declining to invent a destructive mutation the P0 scope does not list — is correct and is exactly what baseline §6 prescribes. But the finding as filed is too narrow: SRC-SPEC §7.3 names **three** operations and the inventory supplies one, with only the third flagged. See `F-A1R1-04`. Raising `PROV-PC01-03` to cover both missing operations, and correcting `save.remove`'s requirement citation, would close the traceability gap without anyone having to invent a destructive operation now.
- **Removed spec §6.2 `COL→AW` edge.** Correctly handled, and consistently across packages. It is a faithful application of baseline §5 B12; PC00 records it as `AMD-B12` with before/after and an oracle change; ADR-0001 states it as decision point 4 with the alternative ("keep the edge") explicitly weighed and rejected on I02/D08 grounds; PC01 realises it as `FE-07`/`FE-08` plus `TD-05`, all `PROVISIONAL` with `decision_owner: Owner`; and it appears in the Owner request under B12 marked **CHẶN M0**. It is labelled a change to a diagram the Owner has read, not slipped through. No finding.
- **`F-PC00-01` (unlink as an in-app action).** I agree with the resolution. D36 (**XN**) fixes three commands and §11.3 repeats "chỉ … 3 lệnh"; D37 (**ĐX**) asks for an unlink command; plan §3 B10 recommends "không tự thêm lệnh Telegram thứ tư". Resolving a **ĐX** row in favour of an **XN** row, and following the plan's explicit recommendation, is the correct precedence under SRC-PLAN §1. It is well-traced: `REQ-D37`'s `notes` record the conflict and the provisional choice, `AMD-B10` carries the before/after, `telegram.unlink` is an `owner_session` HTTP operation callable only by `MOD-web-ui`, and PC00 and PC01 reached it independently (`F-PC00-01` and `PROV-PC01-02`) — convergence, not coordination. It is in the Owner request. No finding.
- **`F-PC00-02` (Saved export deferred to P1).** The decision itself is sound — it adopts baseline §5's "Saved export deferred to P1 (PROVISIONAL)" verbatim, and PC01 marks `save.export` `deferred_p1` while still naming the operation so later packages can cite it, which is the right way to defer without erasing. My reservation is traceability only: the requirement row that carries the export obligation was not annotated. See `F-A1R1-09`. This is the weaker of the two self-made decisions in terms of paper trail, and `CR-PC00-02`'s request that PC09 track both findings is well-founded.

## 7. Verdicts

| Scope | Verdict | Basis |
| --- | --- | --- |
| **PC00** (`precode/*`) | **PASS** | Every packet checklist item verified independently and reproduced: 246 atomic rows with 0 duplicate IDs and 0 ID-form violations; complete coverage of all 59 decision rows, AC-01–18, P0-01–12, P1, OOS, A1–A7, OQ01–10 and all 28 required prose sections; **0 status-fidelity deviations** from the spec; all 246 source anchors resolve and every anchor line number checks out against the real source bytes; the 25-row fidelity sample is 25/25 faithful; B01–B17 each present once, all `PROVISIONAL`, all matching baseline §5 with quoted conflict, oracle change and "if Owner rejects"; 14 amendments each with all six required fields; 10 well-formed ADRs consistent with the register and with PC01/PC02's actual choices; a complete 20-section Owner request declaring `grants_authority: false`. Source copies are byte-identical to the pinned sources. The four findings against it (`F-A1R1-05/06/08/09`) are MINOR traceability and header defects; none blocks the scope's purpose. |
| **PC01** (`contracts/modules.yaml`, `capabilities.yaml`, `ports.yaml`, `ops/deployment.md`) | **FAIL** | Its own oracles all reproduce exactly — 82 unique operations, 96 allowed edges set-equal to the derived caller×owner product, 56 mutations each with exactly one owner scope, 36 reasoned forbidden edges, 10 complete denied cases, all 16 plan §10 error codes, complete against the packet's minimum operation list, default deny stated. But three evidenced defects sit in its deliverables: an operation that contradicts the frozen invariant contract with no transaction of its own (`F-A1R1-01`), an ownership table that leaves the B15/I03 audit trail unowned and diverges from the entity contract on nine names without PC01 reporting any of it (`F-A1R1-03`), and an XN requirement two-thirds uncovered with only one third flagged plus a mis-cited requirement ref (`F-A1R1-04`). Under `auditor.md` these are violations with evidence, so PASS cannot be signed for this scope. |
| **PC02** (`contracts/data/*`, `contracts/schemas/*`, `acceptance/fixtures/identity/*`) | **FAIL** | The contract work is the strongest in the candidate and its self-validation reproduces in full — both schemas pass the 2020-12 metaschema, 39 target objects validate, all 9 batch accept/reject outcomes reproduce for the stated reasons, 4/4 positive payload hashes recompute, all 18 spec §7.1 entities and all 15 packet additions present, `owner_id` everywhere but the two declared exceptions, UNIQUE guarantees stated with explicit non-guarantees, the Saved constraint correctly on the union key, every limit numbered with unit and rationale, I02/I03/I04/I08 each with oracle and multiple counterexamples, and all eight fixtures arithmetically consistent. It fails on delivered artefacts nonetheless: 10 of 22 fixture events assert communication edges the frozen registry forbids (`F-A1R1-02`) — the very check PC02 identified as missing and did not run — and it shares `F-A1R1-01` and `F-A1R1-03`. |
| **Overall FC-W1** | **FAIL** | Manifest integrity, source fidelity and evidence honesty are sound, and the candidate holds together at `DRAFT_FOR_REVIEW` in most respects. It fails on four MAJOR cross-package defects that a new epoch must clear. `STALE` does not apply: the snapshot was byte-identical at start and end. `BLOCKED` does not apply: every artefact needed to conclude was readable at the frozen bytes. |

**This PASS/FAIL is scoped to what I checked and is not product acceptance.** Per protocol §9, `NOT_READY_FOR_PRODUCT_CODE` stands and B01–B17 remain `OPEN` in `agent_profile/registry.json`.

## 8. Limitations

1. **E0 only.** Everything I verified is static consistency of contract text. Zero runtime evidence exists in this candidate and none was claimed. A fixture that parses does not prove a guard runs; 36 forbidden edges with reasons do not prove any edge is blocked; the ten denied cases are `NOT_RUN` by the writers' own admission and remain so. E1–E4 are `NOT_RUN` and my PASS verdicts cannot be read as evidence about behaviour.
2. **Provenance is asserted, not proven.** I confirmed the two source copies are byte-identical to the pinned sources, but hash equality does not prove those sources are the documents the Owner attached; that provenance rests on the registry pin and baseline §2, which I took as given.
3. **Fidelity sampling.** I read 25 of 246 requirement rows against the sources in full (the packet's floor) plus all 17 blockers, all 14 amendments and all 10 ADRs. The remaining 221 rows were checked structurally — anchor resolution, status enum, ID form, package/blocker enums — not word-by-word against the sources. A paraphrase that drifts in an unsampled row would not have been caught.
4. **Reference checks catch dangling references, not missing ones.** Like PC02's own EV-PC02-03, my cross-checks prove that what is written resolves; they cannot prove that a relationship someone should have written down exists. `F-A1R1-03`'s reverse direction (29 owner tokens with no entity) is the visible part of that class; there may be more.
5. **Semantic correctness of the transaction and identity designs was assessed by reading, not by execution.** I judged the merge algorithm, quarantine semantics, failure timelines and invariant oracles to be coherent and well-grounded in the sources, but no walkthrough was executed against a database and SQLite's actual support for the partial indexes and STORED generated columns the design relies on is untested — PC02 discloses this as its own risk 1.
6. **Writer quiescence was inferred, not enforced.** Under `DOCUMENTARY_DRAFT` there is no OS enforcement; I relied on the three released-lease declarations plus my own start/end rehash showing zero drift across the review interval. That is quiescence by observation, not a runtime fence, exactly as protocol §2 requires be stated.
7. **PC03/PC04 were running in parallel and are excluded.** No file under `contracts/state/` or `contracts/reporting/` existed at any point during my audit, so the parallel work did not perturb the snapshot — but several findings (`F-A1R1-01`, `F-A1R1-03`, `F-A1R1-04`) name obligations those packages will inherit, and my verdicts say nothing about how they discharge them.

## 9. Completion ceiling statement

The maximum claim supportable by this audit is **`DRAFT_FOR_REVIEW`**, the candidate's declared ceiling, and I do not sign it for PC01 or PC02 while `F-A1R1-01` … `F-A1R1-04` stand. No file in the candidate asserts more, which is correct. Specifically **not** established: `CONTRACT_READY` for any scope (B01–B17 are `OPEN`, and the plan's G1/G2 conditions are not met while the checkpoint contradiction and the ownership holes persist); `IMPLEMENTATION_VERIFIED` or beyond (no code exists); any claim about X, Telegram, AI providers, or SQLite behaviour (nothing was executed).

All nine findings are **OPEN**. Per protocol §8 I neither close them nor accept risk on them; a Worker may return remediation evidence under a new packet, but disposition to `CLOSED` requires the designated authority after independent verification of a **new frozen epoch**, and that verification may not be performed by the principal who authored the fix. I have not been assigned a re-review turn and will not continue tracking this candidate unless dispatched.

---
*Produced by `auditor-A1` under `AUTH-COORD-A1-R1` / `PKT-A1-R1`. Read-only; no repository bytes were written, and no candidate bytes were supplied for a later self-audit. Review type: `INDEPENDENT_AUDIT`.*
