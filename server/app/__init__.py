"""Research Radar server application package (`server.app`).

Domain subpackages (`auth/`, `ingest/`, `identity/`, `storage/`, `health/`, ...) are created
by the task cards that own them (agent-tasks/**, §3). Phase 0 deliberately ships only the
app factory and the database engine factory.
"""
